---
phase: quick
plan: 260331-089
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - src/engine/core/MathUtils.h
  - src/game/content/ParseUtils.h
  - src/editor/scene/EditorSceneDocument.cpp
  - src/editor/scene/EditorSelectionSystem.cpp
  - src/editor/render/EditorAssetPreviewRenderer.cpp
  - src/editor/render/EditorScenePreviewRenderer.cpp
  - src/editor/viewport/EditorViewportInteraction.cpp
  - src/engine/rendering/SceneRenderPipeline.cpp
  - src/engine/rendering/assets/AssimpLoader.cpp
  - src/game/level/LevelDef.cpp
  - src/game/rendering/RuntimeSceneRenderer.cpp
  - src/game/rendering/EnvironmentDebugSync.cpp
  - src/game/content/ContentRegistry.cpp
  - src/game/rendering/MaterialDefinition.cpp
  - src/game/rendering/EnvironmentDefinition.cpp
autonomous: true
requirements: []
must_haves:
  truths:
    - "Project builds successfully with no safeNormalize/makeTransformMatrix/parser local duplicates"
    - "ImGui CMake source patching is removed and scroll behavior is unchanged (GLFW-level fix remains)"
    - "All 10 safeNormalize call sites compile and produce identical behavior"
    - "All 4 parser files use shared ParseUtils.h with identical behavior"
  artifacts:
    - path: "src/engine/core/MathUtils.h"
      provides: "safeNormalize and makeTransformMatrix inline functions"
      contains: "safeNormalize"
    - path: "src/game/content/ParseUtils.h"
      provides: "throwParseError, isCommentOrEmpty, tokenizeRecord inline functions"
      contains: "throwParseError"
  key_links:
    - from: "src/editor/scene/EditorSceneDocument.cpp"
      to: "src/engine/core/MathUtils.h"
      via: "#include"
      pattern: '#include "engine/core/MathUtils.h"'
    - from: "src/game/content/ContentRegistry.cpp"
      to: "src/game/content/ParseUtils.h"
      via: "#include"
      pattern: '#include "game/content/ParseUtils.h"'
---

<objective>
Remove the redundant ImGui CMake source patch, and deduplicate three sets of copy-pasted utility functions (safeNormalize, makeTransformMatrix, parser helpers) into shared inline headers.

Purpose: Eliminate maintenance burden of 10+ identical function copies that drift over time (3 variants of safeNormalize already exist). Remove fragile CMake source patching now that GLFW-level scroll fix exists.
Output: Two new header files (MathUtils.h, ParseUtils.h), cleaned CMakeLists.txt, 14 source files updated.
</objective>

<execution_context>
@.planning/quick/260331-089-deduplicate-shared-utils-and-remove-cmak/260331-089-PLAN.md
</execution_context>

<context>
@CLAUDE.md
@src/engine/CMakeLists.txt
@src/game/CMakeLists.txt
@CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove ImGui CMake source patch and create MathUtils.h</name>
  <files>CMakeLists.txt, src/engine/core/MathUtils.h</files>
  <action>
**1a. Remove ImGui CMake source patching (CMakeLists.txt lines 54-57)**

Delete these 4 lines from the root CMakeLists.txt (the comment on line 54 and the 3 code lines 55-57):
```
# Patch ImGui scroll speed: reduce Y-axis from 5 lines/notch to 1 (hardcoded in UpdateMouseWheel)
file(READ "${imgui_SOURCE_DIR}/imgui.cpp" _imgui_src)
string(REPLACE "ImMin(5 * window->FontRefSize" "ImMin(1 * window->FontRefSize" _imgui_src "${_imgui_src}")
file(WRITE "${imgui_SOURCE_DIR}/imgui.cpp" "${_imgui_src}")
```

Also delete the comment on line 58: `# imgui_SOURCE_DIR is set after MakeAvailable`

This is safe because commit 58d5760 added GLFW-level scroll scaling in ImGuiLayer.cpp via `AddMouseWheelEvent`, making the source-level patch redundant.

**1b. Create src/engine/core/MathUtils.h**

Create a header-only utility file with `#pragma once`. Include `<glm/glm.hpp>`, `<glm/gtc/matrix_transform.hpp>`. Place functions in a `namespace MathUtils` to avoid ADL surprises, using `inline` linkage.

Two functions:

1. `inline glm::vec3 safeNormalize(const glm::vec3& value, const glm::vec3& fallback)` — Use the standard implementation: `glm::dot(value, value) <= 0.0001f` threshold with `glm::normalize(value)` return. This is the implementation used by 8 of 10 call sites.

2. `inline glm::mat4 makeTransformMatrix(const glm::vec3& position, const glm::vec3& rotationDegrees, const glm::vec3& scale)` — Translate, then rotate X/Y/Z in that order using `glm::radians`, then scale. Identical to the implementation in LevelDef.cpp and EditorSceneDocument.cpp.

Do NOT put these in a namespace — keep them as free functions in global scope (matching the existing anonymous-namespace pattern so call sites need zero changes beyond removing the local definition). No namespace wrapping.

No .cpp file needed. No CMake changes needed for engine_core — the PUBLIC include directory already covers header-only files in src/engine/core/.
  </action>
  <verify>
    <automated>cd /Users/ozan/Projects/gsd-3d-roguelike && cmake --build build --target engine_core 2>&1 | tail -5</automated>
  </verify>
  <done>CMakeLists.txt has no ImGui source patching. MathUtils.h exists with safeNormalize and makeTransformMatrix as inline free functions. engine_core compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all safeNormalize and makeTransformMatrix duplicates with MathUtils.h</name>
  <files>
    src/editor/scene/EditorSceneDocument.cpp,
    src/editor/scene/EditorSelectionSystem.cpp,
    src/editor/render/EditorAssetPreviewRenderer.cpp,
    src/editor/render/EditorScenePreviewRenderer.cpp,
    src/editor/viewport/EditorViewportInteraction.cpp,
    src/engine/rendering/SceneRenderPipeline.cpp,
    src/engine/rendering/assets/AssimpLoader.cpp,
    src/game/level/LevelDef.cpp,
    src/game/rendering/RuntimeSceneRenderer.cpp,
    src/game/rendering/EnvironmentDebugSync.cpp
  </files>
  <action>
Add `#include "engine/core/MathUtils.h"` to all 10 files listed below and remove their local safeNormalize definitions. Include placement: after third-party includes, before other project includes (following project convention).

**safeNormalize removal in each file:**

1. `src/editor/scene/EditorSelectionSystem.cpp` — Remove the free function at line ~15 (5 lines).
2. `src/editor/scene/EditorSceneDocument.cpp` — Remove the free function at line ~58 (5 lines). ALSO remove `makeTransformMatrix` at lines ~22-31 (the 10-line function in the anonymous namespace).
3. `src/editor/render/EditorScenePreviewRenderer.cpp` — Remove the free function at line ~28 (5 lines).
4. `src/editor/viewport/EditorViewportInteraction.cpp` — Remove the free function at line ~23 (5 lines).
5. `src/editor/render/EditorAssetPreviewRenderer.cpp` — Remove the variant at line ~21 (7 lines, uses `1e-6f` and manual sqrt). The shared 0.0001f+glm::normalize version is equivalent for all practical use in light direction normalization.
6. `src/engine/rendering/SceneRenderPipeline.cpp` — Remove the free function at line ~20 (5 lines).
7. `src/game/level/LevelDef.cpp` — Remove the lambda at line ~598 (6 lines inside `resolveLevelHierarchy`). Replace captures/calls: change `safeNormalize(...)` calls in that function to just `safeNormalize(...)` (same name, now resolved from header). ALSO remove `makeTransformMatrix` at lines ~110-119 (the function in the anonymous namespace).
8. `src/game/rendering/RuntimeSceneRenderer.cpp` — Remove the free function at line ~45 (5 lines).
9. `src/game/rendering/EnvironmentDebugSync.cpp` — Remove the free function at line ~9 (5 lines).
10. `src/engine/rendering/assets/AssimpLoader.cpp` — Remove the variant at line ~27 (7 lines, uses `1e-12f` and manual sqrt). The shared version is functionally equivalent — the only difference is threshold sensitivity for near-zero vectors, which doesn't matter for mesh normal/tangent normalization where vectors are either valid or zero.

**makeTransformMatrix removal (2 files only):**

1. `src/editor/scene/EditorSceneDocument.cpp` — Already covered above (lines ~22-31).
2. `src/game/level/LevelDef.cpp` — Already covered above (lines ~110-119).

Do NOT touch `makeModelMatrix` in EditorScenePreviewRenderer.cpp or EditorViewportInteraction.cpp — those have a different parameter order (position, scale, rotation) and are not duplicates of makeTransformMatrix.

For LevelDef.cpp lambda removal: the lambda `auto safeNormalize = [](...)` is defined inside `resolveLevelHierarchy()`. Remove the lambda definition. The free function from MathUtils.h will be found by unqualified lookup at the single call site (line ~645).
  </action>
  <verify>
    <automated>cd /Users/ozan/Projects/gsd-3d-roguelike && cmake --build build 2>&1 | tail -10</automated>
  </verify>
  <done>All 10 files compile using shared safeNormalize from MathUtils.h. Both files using makeTransformMatrix compile using the shared version. No local definitions of either function remain in any source file. Full project builds cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Create ParseUtils.h and replace parser duplicates</name>
  <files>
    src/game/content/ParseUtils.h,
    src/game/content/ContentRegistry.cpp,
    src/game/level/LevelDef.cpp,
    src/game/rendering/MaterialDefinition.cpp,
    src/game/rendering/EnvironmentDefinition.cpp
  </files>
  <action>
**3a. Create src/game/content/ParseUtils.h**

Header-only with `#pragma once`. Include `<string>`, `<vector>`, `<sstream>`, `<stdexcept>`. Three inline free functions (no namespace):

1. `[[noreturn]] inline void throwParseError(const std::string& path, int lineNumber, const std::string& message)` — throws `std::runtime_error(path + ":" + std::to_string(lineNumber) + ": " + message)`.

2. `inline bool isCommentOrEmpty(const std::string& line)` — iterates chars: if `#` return true, if non-whitespace return false, after loop return true. Use `std::isspace(static_cast<unsigned char>(c))` cast (matching existing code).

3. `inline std::vector<std::string> tokenizeRecord(const std::string& line)` — `std::istringstream` extraction loop into vector.

No CMake changes needed — `game_content` already has PUBLIC include on `${CMAKE_SOURCE_DIR}/src`, so `#include "game/content/ParseUtils.h"` resolves for all targets linking game_content.

**3b. Update 4 source files:**

1. `src/game/content/ContentRegistry.cpp` — Add `#include "game/content/ParseUtils.h"`. Remove local `throwParseError` (lines ~41-43), `isCommentOrEmpty` (lines ~45-55), and `tokenizeRecord` (lines ~57-65). Keep the `loadDefinitionFile` template and `listFilesWithExtension` that are unique to this file.

2. `src/game/level/LevelDef.cpp` — Add `#include "game/content/ParseUtils.h"`. Remove local `throwParseError` (lines ~21-23) and `isCommentOrEmpty` (lines ~25-32). This file does NOT have `tokenizeRecord` — it uses a different line-parsing approach (std::istringstream inline). Do not add tokenizeRecord usage.

3. `src/game/rendering/MaterialDefinition.cpp` — Add `#include "game/content/ParseUtils.h"`. Remove local `throwParseError` (lines ~11-13), `isCommentOrEmpty` (lines ~15-25), and `tokenizeRecord` (lines ~67-75). Keep `tryParseFloatToken`, `parseVec3Record`, `parseVec2Record`, `parseFloatRecord` which are unique to this file.

4. `src/game/rendering/EnvironmentDefinition.cpp` — Add `#include "game/content/ParseUtils.h"`. Remove local `throwParseError` (lines ~13-15), `isCommentOrEmpty` (lines ~17-27), and `tokenizeRecord` (lines ~41-49). Keep `parseBool`, `parseVec3Record`, `parseFloatRecord` which are unique to this file.

Include placement in each file: after standard library includes, before project includes.
  </action>
  <verify>
    <automated>cd /Users/ozan/Projects/gsd-3d-roguelike && cmake --build build 2>&1 | tail -10</automated>
  </verify>
  <done>ParseUtils.h exists with 3 shared parser functions. All 4 consumer files compile using the shared header. No local definitions of throwParseError, isCommentOrEmpty, or tokenizeRecord remain in any of the 4 files. Full project builds cleanly.</done>
</task>

</tasks>

<verification>
After all 3 tasks:
1. `cmake --build build` succeeds with zero errors
2. `grep -rn "safeNormalize" src/ --include="*.cpp" | grep -v "#include" | grep -v "safeNormalize("` — no function definitions remain (only call sites)
3. `grep -rn "void throwParseError" src/ --include="*.cpp"` — returns empty (no local definitions)
4. `grep -n "file(READ.*imgui" CMakeLists.txt` — returns empty (patch removed)
5. Run the game executable to verify scroll behavior is unchanged
</verification>

<success_criteria>
- Full project builds with zero errors and zero new warnings
- src/engine/core/MathUtils.h contains safeNormalize and makeTransformMatrix
- src/game/content/ParseUtils.h contains throwParseError, isCommentOrEmpty, tokenizeRecord
- Zero local definitions of safeNormalize remain in any .cpp file
- Zero local definitions of throwParseError/isCommentOrEmpty/tokenizeRecord remain in the 4 parser files
- CMakeLists.txt contains no ImGui source patching
- No behavioral changes to any system (pure refactor)
</success_criteria>

<output>
After completion, create `.planning/quick/260331-089-deduplicate-shared-utils-and-remove-cmak/260331-089-SUMMARY.md`
</output>
