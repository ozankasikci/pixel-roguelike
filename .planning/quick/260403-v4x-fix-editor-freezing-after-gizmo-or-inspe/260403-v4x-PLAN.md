---
phase: quick
plan: 260403-v4x
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/level_editor/main.cpp
autonomous: true
requirements: []
---

<objective>
Fix editor freezes caused by eager runtime preview rebuilds after gizmo, undo/redo, or inspector scene changes.

Purpose: Preserve the previous play-preview correctness fix, but stop ordinary edit-time scene revisions from triggering a heavyweight background runtime rebuild while the user is still editing.

Output: scene edits no longer stall the editor, while Play Preview still rebuilds from the latest document before entering play.
</objective>

<context>
@apps/level_editor/main.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Separate on-play rebuild invalidation from eager background rebuild</name>
  <files>apps/level_editor/main.cpp</files>
  <action>
Keep the fix narrow.

Introduce a small local flag so the runtime preview can distinguish:
1. scene changes that must be rebuilt before the next Play enter
2. scene changes that are safe to leave pending without immediately rebuilding in the editor loop

Ordinary scene-revision edits from gizmo/undo/redo/inspector should keep marking runtime preview as `FullWorldRebuild`, but they must not trigger the idle background rebuild path that freezes the editor. Structural `previewDirty` changes can keep the existing eager rebuild behavior.
  </action>
  <verify>
    <automated>cd /Users/ozan/Projects/gsd-3d-roguelike && cmake --build build --target level-editor test_editor_runtime_preview && ctest --test-dir build --output-on-failure -R editor_runtime_preview</automated>
  </verify>
  <done>
    Gizmo or inspector-driven scene edits no longer lock the editor a few hundred milliseconds later, and Play Preview still rebuilds from the latest document when entered.
  </done>
</task>

</tasks>

<verification>
- Build `level-editor` and `test_editor_runtime_preview`.
- Run `editor_runtime_preview`.
- Manual sanity check: move a mesh with the gizmo, wait idle in edit mode, and confirm the editor stays responsive instead of hitting the macOS rainbow cursor.
</verification>

<success_criteria>
Ordinary edit-time scene mutations no longer trigger a blocking background runtime preview rebuild, and the previous play-preview stale-state fix remains intact.
</success_criteria>

<output>
After completion, commit the code fix first, then commit the quick-task docs/state bookkeeping.
</output>
