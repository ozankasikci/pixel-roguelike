---
phase: quick
plan: 260402-uz2
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/scenes/initial_scene.scene
  - assets/materials/qdp_wall.material
  - assets/materials/qdp_floor.material
autonomous: true
requirements: []
---

<objective>
Fix walls and floors in initial_scene.scene to use QuestDoorsPack textured materials (qdp_wall, qdp_floor) instead of solid-color procedural materials (inst_beige_wall, inst_glossy_floor). Also fix UV mode on both material files to use mesh UVs (matching other QuestDoorsPack materials).

Purpose: The previous task (260402-uc1) replaced wall/floor meshes with QuestDoorsPack SM_Wall/SM_Floor FBX files and created the qdp_wall/qdp_floor material files, but forgot to update the material references in the scene file. Walls and floors currently render with flat procedural colors instead of the PBR textures.

Output: Scene renders with proper QuestDoorsPack wall and floor textures.
</objective>

<context>
@assets/scenes/initial_scene.scene
@assets/materials/qdp_wall.material
@assets/materials/qdp_floor.material
@assets/materials/qdp_door_a.material (reference for correct uv_mode)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update scene material references and fix material UV modes</name>
  <files>assets/scenes/initial_scene.scene, assets/materials/qdp_wall.material, assets/materials/qdp_floor.material</files>
  <action>
Three changes:

1. **assets/scenes/initial_scene.scene** -- Replace material references on SM_Wall and SM_Floor lines ONLY:
   - All lines starting with `mesh SM_Floor` (nodes n_f01 through n_f20, 20 lines): change `material inst_glossy_floor` to `material qdp_floor`
   - All lines starting with `mesh SM_Wall` (nodes n_ws1-n_ws5, n_wn1-n_wn2, n_wn4-n_wn5, n_ww1, n_ww3-n_ww4, n_we1-n_we2, n_we4 -- 15 lines total): change `material inst_beige_wall` to `material qdp_wall`
   - DO NOT change `prison_wall_door` lines (n_wn3, n_ww2, n_we3) -- these are engine-generated procedural meshes that have no baked UVs, so they must keep `inst_beige_wall` which uses world_projected UV mode
   - DO NOT change any other mesh lines (ceilings, doors, lights, colliders, etc.)

2. **assets/materials/qdp_wall.material** -- Change `uv_mode world_projected` to `uv_mode mesh`. The SM_Wall FBX from QuestDoorsPack has properly baked UVs, consistent with qdp_door_a/c/d materials which all use `uv_mode mesh`.

3. **assets/materials/qdp_floor.material** -- Change `uv_mode world_projected` to `uv_mode mesh`. Same reasoning as walls -- the SM_Floor FBX has baked UVs.
  </action>
  <verify>
    <automated>cd /Users/ozan/Projects/gsd-3d-roguelike && grep -c "material inst_glossy_floor" assets/scenes/initial_scene.scene | grep -q "^0$" && echo "PASS: no inst_glossy_floor in SM_Floor lines" && grep -c "material qdp_floor" assets/scenes/initial_scene.scene | grep -q "^20$" && echo "PASS: 20 qdp_floor references" && grep -c "material qdp_wall" assets/scenes/initial_scene.scene | grep -q "^15$" && echo "PASS: 15 qdp_wall references" && grep -c "prison_wall_door.*inst_beige_wall" assets/scenes/initial_scene.scene | grep -q "^3$" && echo "PASS: 3 prison_wall_door lines still use inst_beige_wall" && grep "uv_mode mesh" assets/materials/qdp_wall.material && grep "uv_mode mesh" assets/materials/qdp_floor.material && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>
    - All 20 SM_Floor lines reference qdp_floor material
    - All 15 SM_Wall lines reference qdp_wall material
    - All 3 prison_wall_door lines still reference inst_beige_wall (unchanged)
    - qdp_wall.material and qdp_floor.material both use uv_mode mesh
  </done>
</task>

</tasks>

<verification>
- Scene file has zero remaining references to inst_glossy_floor
- SM_Wall lines use qdp_wall; prison_wall_door lines still use inst_beige_wall
- Material files use uv_mode mesh matching all other QuestDoorsPack materials
- Build and run editor to visually confirm textured walls and floors (checkpoint not needed -- straightforward text replacement)
</verification>

<success_criteria>
Walls and floors in initial_scene.scene render with QuestDoorsPack PBR textures instead of flat procedural colors. Prison wall door frames retain their procedural material.
</success_criteria>

<output>
After completion, commit changes directly (quick task, no SUMMARY needed).
</output>
