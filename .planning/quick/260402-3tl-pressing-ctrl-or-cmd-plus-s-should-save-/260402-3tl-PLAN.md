---
phase: quick
plan: 260402-3tl
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/level_editor/main.cpp
autonomous: true
requirements: [quick-save-shortcut]
must_haves:
  truths:
    - "Pressing Ctrl+S (Windows/Linux) or Cmd+S (macOS) saves the current scene in the editor"
    - "Save shortcut works regardless of which panel has focus"
    - "Save shortcut does not fire when typing in a text field (WantTextInput guard not needed for Ctrl+S since it never produces text, but must not conflict)"
  artifacts:
    - path: "apps/level_editor/main.cpp"
      provides: "Ctrl/Cmd+S keyboard shortcut handler"
      contains: "ImGuiKey_S"
  key_links:
    - from: "keyboard shortcut handler"
      to: "savePressed flag"
      via: "IsKeyPressed(ImGuiKey_S) with KeyCtrl/KeySuper modifier"
      pattern: "ImGui::IsKeyPressed\\(ImGuiKey_S\\)"
---

<objective>
Wire up the Ctrl/Cmd+S keyboard shortcut to save the scene in the level editor.

Purpose: The menu item File > Save already shows "Ctrl/Cmd+S" as a hint label, and the `savePressed` flag + save logic already exist, but the actual keyboard shortcut is not bound as a global key handler. This is a one-line fix following the exact same pattern as Ctrl+D (duplicate), Ctrl+Z (undo), and Ctrl+B (build).

Output: Working Ctrl+S / Cmd+S keyboard shortcut that saves the scene.
</objective>

<execution_context>
@.planning/quick/260402-3tl-pressing-ctrl-or-cmd-plus-s-should-save-/260402-3tl-PLAN.md
</execution_context>

<context>
@apps/level_editor/main.cpp (lines 439-468 — global shortcut handlers; line 377 — savePressed declaration; lines 1901-1910 — save execution)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Ctrl/Cmd+S keyboard shortcut for scene save</name>
  <files>apps/level_editor/main.cpp</files>
  <action>
In `apps/level_editor/main.cpp`, inside the `if (!gameplayPreviewCaptured)` block (around line 443-468), add a single line to wire Ctrl/Cmd+S to the existing `savePressed` flag:

```cpp
if ((io.KeyCtrl || io.KeySuper) && ImGui::IsKeyPressed(ImGuiKey_S)) savePressed = true;
```

Place it after the Ctrl+D duplicate shortcut (line 446) and before the Ctrl+Z undo shortcut (line 447), following the same pattern. No `!io.WantTextInput` guard is needed because Ctrl+S does not produce text input — this matches the pattern used by Ctrl+D and Ctrl+Z which also omit the guard.

The save logic already exists at line 1901-1910 (sets `document.save(content)`, `commandStack.markSaved(document)`, resets `savePressed`). No changes needed there.

Do NOT add any additional logging, UI feedback, or status bar changes. The existing save infrastructure handles everything.
  </action>
  <verify>
    <automated>cd /Users/ozan/Projects/gsd-3d-roguelike && cmake --build build --target level-editor -j$(sysctl -n hw.ncpu) 2>&1 | tail -5</automated>
  </verify>
  <done>Ctrl+S and Cmd+S trigger scene save in the editor. The shortcut works from any panel when gameplay preview is not captured. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
- Build succeeds: `cmake --build build --target level-editor`
- Grep confirms the shortcut is wired: `grep -n "ImGuiKey_S" apps/level_editor/main.cpp`
- The shortcut line follows the same pattern as Ctrl+D, Ctrl+Z, Ctrl+B (modifier check + IsKeyPressed)
</verification>

<success_criteria>
- Ctrl+S (Linux/Windows) and Cmd+S (macOS) saves the current scene
- The shortcut respects the `!gameplayPreviewCaptured` guard like all other editor shortcuts
- No regression in existing save behavior (menu item, toolbar button, build-save modal)
- Build compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260402-3tl-pressing-ctrl-or-cmd-plus-s-should-save-/260402-3tl-SUMMARY.md`
</output>
