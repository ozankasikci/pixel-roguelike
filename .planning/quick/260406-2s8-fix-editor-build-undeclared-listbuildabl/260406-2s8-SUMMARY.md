---
phase: quick
plan: 260406-2s8
subsystem: editor/build
tags: [build-system, regression-fix, editor]
dependency_graph:
  requires: []
  provides: [listBuildableScenes, EditorBuildConfig.buildScenes, collectUsedAssets-filter]
  affects: [apps/level_editor/main.cpp]
tech_stack:
  added: []
  patterns: [selective-asset-packaging]
key_files:
  created: []
  modified:
    - src/editor/build/EditorBuildSystem.h
    - src/editor/build/EditorBuildSystem.cpp
decisions:
  - Used -f flag for git add because src/editor/build/ is matched by build/ gitignore pattern, but files are already tracked
metrics:
  duration: ~3 minutes
  completed: "2026-04-06"
  tasks_completed: 1
  files_modified: 2
---

# Phase quick Plan 260406-2s8: Fix Editor Build Undeclared listBuildableScenes Summary

**One-liner:** Restored build-scenes feature (listBuildableScenes, EditorBuildConfig.buildScenes, filtered collectUsedAssets, selective scene packaging, build_scenes persistence) accidentally reverted by commit 3b7addf.

## What Was Done

Commit 3b7addf ("Add Open Build Folder menu item") accidentally reverted the build-scenes feature from commit ff3394f, causing 20+ compilation errors in `apps/level_editor/main.cpp` which references `buildConfig.buildScenes` and `listBuildableScenes()`.

Restored all changes from ff3394f to both files:

**EditorBuildSystem.h:**
- Added `std::vector<std::string> buildScenes;` member to `EditorBuildConfig` struct
- Added `listBuildableScenes()` declaration
- Changed `collectUsedAssets()` signature to accept `const std::vector<std::string>& sceneFilter = {}`

**EditorBuildSystem.cpp:**
- Added `listBuildableScenes()` implementation (enumerates .scene files, sorts alphabetically)
- Added scene filter logic in `collectUsedAssets()` step 3
- Removed `"scenes"` from `alwaysCopyDirs` in `packageGame()`
- Added selective scene-copying block (respects `config.buildScenes`, logs N-of-M scenes)
- Changed `collectUsedAssets()` call to pass `config.buildScenes`
- Added `build_scenes=` parsing in `loadBuildConfig()`
- Added `build_scenes=` writing in `saveBuildConfig()`

## Verification

- `cmake --build build --target level-editor --parallel` completed with exit code 0
- `grep -c "buildScenes" EditorBuildSystem.h` returned 1
- `grep -c "listBuildableScenes" EditorBuildSystem.cpp` returned 3 (comment header + impl + call site)

## Commits

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Restore build-scenes feature | fe042d7 | EditorBuildSystem.h, EditorBuildSystem.cpp |

## Deviations from Plan

**1. [Rule 3 - Blocking] Used git add -f for ignored path**
- **Found during:** Task 1 commit
- **Issue:** `.gitignore` pattern `build/` matches `src/editor/build/` at any depth, blocking `git add`
- **Fix:** Used `git add -f` since both files are already tracked by git (pre-existing tracked files)
- **Files modified:** n/a (git operation only)
- **Commit:** fe042d7

## Known Stubs

None.

## Self-Check: PASSED

- src/editor/build/EditorBuildSystem.h — exists and modified
- src/editor/build/EditorBuildSystem.cpp — exists and modified
- Commit fe042d7 — verified in git log
