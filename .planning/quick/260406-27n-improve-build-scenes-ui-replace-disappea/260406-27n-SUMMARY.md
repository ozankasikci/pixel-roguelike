---
phase: quick
plan: 260406-27n
subsystem: editor/build-ui
tags: [imgui, editor, build-system, ux]
key-files:
  modified:
    - apps/level_editor/main.cpp
decisions:
  - Used "Build Scenes" as both OpenPopup ID and BeginPopupModal title so the X button on title bar closes correctly
  - Used BeginPopupModal (not BeginPopup) for focus-grabbing behavior with title bar
  - "Select All" button uses empty=all convention; individual checkboxes handle per-scene toggling
metrics:
  completed: "2026-04-06"
  tasks: 1
  files: 1
---

# Quick 260406-27n: Improve Build Scenes UI — Replace Disappearing Submenu

Replace the Build > Build Scenes submenu (which closed on every click) with a persistent ImGui popup modal supporting multi-scene checkbox toggling without dismissal.

## What Was Built

- Added `buildScenesPopupRequested` bool flag near other popup flags (~line 424)
- Replaced `BeginMenu("Build Scenes")` block (~50 lines) with a single `MenuItem("Build Scenes...")` that sets the flag
- Added `BeginPopupModal("Build Scenes")` with:
  - "All Scenes (default)" checkbox at top (toggles empty=all convention)
  - Per-scene checkboxes with correct toggle logic
  - "Select All" button for quick reset to all
  - Summary text: "All N scenes selected" (green) or "N of M scenes selected"
  - Close button at bottom + X button on title bar
  - `buildConfig.buildScenes` empty=all convention fully preserved

## Commits

| Task | Description | Hash | Files |
|------|-------------|------|-------|
| 1 | Replace Build Scenes submenu with persistent popup modal | 371d267 | apps/level_editor/main.cpp |

## Deviations from Plan

None — plan executed exactly as written. The plan's draft code had the popup ID mismatch (OpenPopup "BuildScenesPopup" vs BeginPopupModal "Build Scenes") — unified both to "Build Scenes" so the X button on the title bar works correctly.

## Self-Check: PASSED

- [x] `apps/level_editor/main.cpp` modified
- [x] Commit `371d267` exists
- [x] Build succeeded: `[100%] Built target level-editor`
- [x] Old `BeginMenu("Build Scenes")` block removed
- [x] New `BeginPopupModal("Build Scenes")` added with all required checkboxes
