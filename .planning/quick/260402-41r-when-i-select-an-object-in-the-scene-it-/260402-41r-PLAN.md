---
phase: quick
plan: 260402-41r
type: execute
wave: 1
depends_on: []
files_modified:
  - src/editor/ui/LevelEditorUi.h
  - src/editor/ui/EditorOutlinerPanel.cpp
  - apps/level_editor/main.cpp
autonomous: true
requirements: [quick-task]

must_haves:
  truths:
    - "When user clicks an object in the 3D viewport, the outliner panel scrolls to reveal that object"
    - "If the selected object is inside a collapsed group, the group tree node auto-expands"
    - "Outliner-originated selections do NOT trigger a redundant scroll"
  artifacts:
    - path: "src/editor/ui/LevelEditorUi.h"
      provides: "scrollToSelection flag on EditorUiState"
      contains: "scrollToSelection"
    - path: "src/editor/ui/EditorOutlinerPanel.cpp"
      provides: "Auto-scroll and auto-expand logic in renderOutliner"
      contains: "SetScrollHereY"
    - path: "apps/level_editor/main.cpp"
      provides: "Sets scrollToSelection=true on viewport selection changes"
      contains: "scrollToSelection"
  key_links:
    - from: "apps/level_editor/main.cpp"
      to: "EditorUiState::scrollToSelection"
      via: "set flag when viewport click changes selection"
      pattern: "scrollToSelection\\s*=\\s*true"
    - from: "src/editor/ui/EditorOutlinerPanel.cpp"
      to: "EditorUiState::scrollToSelection"
      via: "consume flag, call SetScrollHereY on first selected node, reset flag"
      pattern: "SetScrollHereY"
---

<objective>
Auto-scroll the outliner/hierarchy panel to the selected object when the user selects something in the 3D viewport.

Purpose: Currently, selecting an object in the viewport does not scroll the outliner to show it, forcing users to manually hunt through the hierarchy. This is a standard UX pattern in every 3D editor (Blender, Unity, Unreal).

Output: Modified EditorUiState, EditorOutlinerPanel, and main.cpp to implement scroll-to-selection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/editor/ui/LevelEditorUi.h
@src/editor/ui/EditorOutlinerPanel.cpp
@src/editor/ui/EditorOutlinerPanel.h
@apps/level_editor/main.cpp
@src/editor/scene/EditorSceneDocument.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scrollToSelection flag and set it on viewport selection</name>
  <files>src/editor/ui/LevelEditorUi.h, apps/level_editor/main.cpp</files>
  <action>
1. In `EditorUiState` (LevelEditorUi.h), add a new bool field:
   ```
   bool scrollToSelection = false;
   ```
   Place it near the existing `frameSelectionRequested` field (around line 81).

2. In `apps/level_editor/main.cpp`, set `ui.scrollToSelection = true` at every point where a viewport click changes the selection. There are two key locations:

   a. After `applySelectionHit(selectedIds, selectionPicker, io.KeyShift)` around line 1709 — add `ui.scrollToSelection = true;` right after the existing `ui.inspectorContext = EditorInspectorContext::SceneSelection;` line.

   b. After placement commits that set `selectedIds = { *placedId }` (around lines 1663 and 1689) — add `ui.scrollToSelection = true;` after each.

   Do NOT set the flag in the outliner panel itself (clicks in the outliner should not trigger auto-scroll since the user is already looking at the item).

   Do NOT set the flag when the user clears selection by clicking empty space (line 1717 area) — no scroll needed for deselection.
  </action>
  <verify>
    <automated>cd /Users/ozan/Projects/gsd-3d-roguelike && cmake --build build --target level-editor 2>&1 | tail -5</automated>
  </verify>
  <done>EditorUiState has scrollToSelection field, main.cpp sets it on viewport selection changes, project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Implement auto-expand and scroll-to in the outliner panel</name>
  <files>src/editor/ui/EditorOutlinerPanel.cpp</files>
  <action>
In `renderOutliner()` in EditorOutlinerPanel.cpp, implement scroll-to-selection with ancestor auto-expand.

1. **Before the tree rendering loop** (after the `ImGui::Separator()` around line 86, before the `renderNode` lambda definition), add ancestor-expand logic when `ui.scrollToSelection` is true:

   - Build a set of ancestor IDs that need to be force-opened. For each id in `selectedIds`, walk up via `document.parentObjectId(id)` collecting all ancestor IDs into an `std::unordered_set<std::uint64_t> expandForScroll`.

2. **Inside the `renderNode` lambda**, before the `ImGui::TreeNodeEx` call:

   - If `expandForScroll.contains(objectId)` is true, call `ImGui::SetNextItemOpen(true, ImGuiCond_Always)` to force-expand ancestor nodes that contain the selected item.

3. **After the `ImGui::TreeNodeEx` call** (and after the existing click-handling block, around line 147 where pushSelectionCommand is called):

   - If `ui.scrollToSelection` is true AND `isSelected(selectedIds, objectId)` is true AND we haven't scrolled yet this frame (use a local bool `scrolledThisFrame`), call `ImGui::SetScrollHereY(0.5f)` to center the item vertically in the outliner, then set `scrolledThisFrame = true`.

4. **After the tree rendering loop completes** (after the root drop zone, before `ImGui::End()`), reset the flag:
   ```
   ui.scrollToSelection = false;
   ```

   The flag is consumed once per frame — set by main.cpp on viewport selection, consumed by the outliner render, reset at end.

Note: `expandForScroll` must be captured in the `renderNode` lambda. Since it's defined in the outer scope as a local variable, it will be captured by the existing `[&]` capture.

Note: `scrolledThisFrame` should also be a local bool in the outer scope (before the lambda), initialized to false, captured by reference.
  </action>
  <verify>
    <automated>cd /Users/ozan/Projects/gsd-3d-roguelike && cmake --build build --target level-editor 2>&1 | tail -5</automated>
  </verify>
  <done>Selecting an object in the viewport scrolls the outliner to reveal it. Collapsed parent groups auto-expand. Outliner clicks do not trigger redundant scrolling.</done>
</task>

</tasks>

<verification>
1. Build succeeds: `cmake --build build --target level-editor`
2. Manual test: Open level editor, load a scene with several objects (some inside groups). Click an object in the 3D viewport — outliner should scroll to show that object highlighted. If the object is inside a collapsed group, the group should auto-expand first.
3. Manual test: Click an object directly in the outliner — no jittery scroll should occur (flag is NOT set from outliner clicks).
4. Manual test: Click empty space in viewport to deselect — no scroll should occur.
</verification>

<success_criteria>
- Viewport object selection scrolls outliner to the selected item
- Collapsed ancestor groups auto-expand to reveal nested selected items
- No scroll triggered by outliner-internal clicks
- No scroll triggered by deselection
- Clean compile with no warnings
</success_criteria>

<output>
After completion, create `.planning/quick/260402-41r-when-i-select-an-object-in-the-scene-it-/260402-41r-SUMMARY.md`
</output>
