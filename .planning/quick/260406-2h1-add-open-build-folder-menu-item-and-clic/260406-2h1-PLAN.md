---
phase: quick
plan: 260406-2h1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/editor/build/EditorBuildSystem.h
  - src/editor/build/EditorBuildSystem.cpp
  - apps/level_editor/main.cpp
autonomous: true
must_haves:
  truths:
    - "Build menu has 'Open Build Folder' item that opens the build directory in the OS file manager"
    - "Build Output panel shows a clickable 'Open Folder' button after a successful build or package"
  artifacts:
    - path: "src/editor/build/EditorBuildSystem.h"
      provides: "openFolderInOS() utility declaration"
    - path: "src/editor/build/EditorBuildSystem.cpp"
      provides: "openFolderInOS() cross-platform implementation"
    - path: "apps/level_editor/main.cpp"
      provides: "Menu item and Build Output button wiring"
  key_links:
    - from: "apps/level_editor/main.cpp"
      to: "src/editor/build/EditorBuildSystem.h"
      via: "openFolderInOS() call"
      pattern: "openFolderInOS"
---

<objective>
Add two ways to open the build output folder from the editor: (1) a menu item in the Build menu, and (2) a clickable button in the Build Output panel after a successful build.

Purpose: Quick access to build artifacts without manually navigating to the build directory in Finder/Explorer.
Output: Modified Build menu with "Open Build Folder" item, Build Output panel with "Open Folder" button on success.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/editor/build/EditorBuildSystem.h
@src/editor/build/EditorBuildSystem.cpp
@apps/level_editor/main.cpp

Key existing patterns:
- `revealInFinder()` in `src/editor/ui/EditorAssetBrowserPanel.cpp` (anonymous namespace, lines 19-28) shows cross-platform file-open pattern using `open -R` on macOS, `explorer` on Windows, `xdg-open` on Linux
- Build menu is at line ~750 of main.cpp inside the main menu bar
- Build Output panel is at line ~2240 of main.cpp
- `buildConfig.buildDir` (default "build") is the CMake build directory
- "build-package" is the hardcoded package output directory
- `buildState.exitCode == 0` indicates success; `buildState.packageAfterBuild` was the last operation
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add openFolderInOS utility and Build menu item</name>
  <files>src/editor/build/EditorBuildSystem.h, src/editor/build/EditorBuildSystem.cpp, apps/level_editor/main.cpp</files>
  <action>
1. In `src/editor/build/EditorBuildSystem.h`, add a new free function declaration after the existing `saveBuildConfig` declaration (bottom of file, before the closing `#pragma once` scope):
   ```
   /// Open a folder in the platform's file manager (Finder on macOS, Explorer on Windows, xdg-open on Linux).
   void openFolderInOS(const std::string& folderPath);
   ```

2. In `src/editor/build/EditorBuildSystem.cpp`, implement `openFolderInOS` at the bottom of the file (before `loadBuildConfig` or after `saveBuildConfig`). Follow the same cross-platform pattern as `revealInFinder()` in EditorAssetBrowserPanel.cpp, but open a directory rather than revealing a file:
   - macOS: `open "{absolutePath}" &`
   - Windows: `explorer "{absolutePath}"`
   - Linux: `xdg-open "{absolutePath}" &`
   Use `std::filesystem::absolute()` to resolve relative paths (like "build") to absolute paths before passing to the system command. Use `std::system()` to execute.

3. In `apps/level_editor/main.cpp`, in the Build menu section (around line 770, after the "Build Scenes..." menu item and before the "Configuration" submenu):
   - Add a separator after the "Build Scenes..." item
   - Add `ImGui::MenuItem("Open Build Folder")` that calls `openFolderInOS(buildConfig.buildDir)` when clicked
   - This item should NOT be disabled during builds (user should be able to open folder anytime)

4. In the Build Output panel (around line 2251-2254, where "Build Succeeded" is shown), add a "Open Folder" button on the same line:
   - After the "Build Succeeded" text, add `ImGui::SameLine()` followed by `if (ImGui::SmallButton("Open Folder")) { openFolderInOS(buildConfig.buildDir); }`
   - This button appears only when the build succeeded (`buildState.exitCode == 0 && buildLog.lineCount() > 1`)
   - The button should use `ImGui::SmallButton` to keep it compact in the header bar
  </action>
  <verify>
    <automated>cd /Users/ozan/Projects/gsd-3d-roguelike && cmake --build build --target level-editor -j$(sysctl -n hw.ncpu) 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Build menu contains "Open Build Folder" menu item after "Build Scenes..." that opens buildConfig.buildDir in Finder
    - Build Output panel shows "Open Folder" SmallButton next to "Build Succeeded" text when last build succeeded
    - Both use the new openFolderInOS() utility from EditorBuildSystem
    - Project compiles without errors
  </done>
</task>

</tasks>

<verification>
- `level-editor` target compiles successfully
- grep confirms `openFolderInOS` is declared in EditorBuildSystem.h, implemented in EditorBuildSystem.cpp, and called from main.cpp
- grep confirms "Open Build Folder" menu item string in main.cpp
- grep confirms "Open Folder" button string in main.cpp Build Output section
</verification>

<success_criteria>
- The Build menu has an "Open Build Folder" item that opens the build directory
- The Build Output panel shows an "Open Folder" button after a successful build
- Both use a shared cross-platform openFolderInOS() utility in EditorBuildSystem
- The project compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260406-2h1-add-open-build-folder-menu-item-and-clic/260406-2h1-SUMMARY.md`
</output>
