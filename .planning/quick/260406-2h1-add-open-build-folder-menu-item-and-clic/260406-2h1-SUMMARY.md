---
phase: quick
plan: 260406-2h1
subsystem: editor/build
tags: [editor, build, ui, cross-platform]
dependency_graph:
  requires: []
  provides: [openFolderInOS utility, Build menu "Open Build Folder" item, Build Output "Open Folder" button]
  affects: [apps/level_editor/main.cpp, src/editor/build/EditorBuildSystem]
tech_stack:
  added: []
  patterns: [cross-platform shell invocation via std::system, std::filesystem::absolute for path resolution]
key_files:
  created: []
  modified:
    - src/editor/build/EditorBuildSystem.h
    - src/editor/build/EditorBuildSystem.cpp
    - apps/level_editor/main.cpp
decisions:
  - Used -f flag with git add to override worktree .gitignore matching src/editor/build/ via unanchored build/ pattern
metrics:
  duration: ~5 minutes
  completed: 2026-04-06
  tasks_completed: 1
  files_modified: 3
---

# Phase quick Plan 260406-2h1: Add Open Build Folder Menu Item and Button Summary

Cross-platform openFolderInOS() utility added to EditorBuildSystem; wired to a Build menu item and a Build Output panel button for one-click access to build artifacts.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add openFolderInOS utility and Build menu item | 3b7addf | EditorBuildSystem.h, EditorBuildSystem.cpp, main.cpp |

## What Was Built

- `openFolderInOS(const std::string& folderPath)` free function in `EditorBuildSystem`: resolves the path to absolute via `std::filesystem::absolute`, then invokes `open` (macOS), `explorer` (Windows), or `xdg-open` (Linux) via `std::system`
- "Open Build Folder" `ImGui::MenuItem` added to the Build menu after "Build Scenes..." with its own separator, calls `openFolderInOS(buildConfig.buildDir)`
- "Open Folder" `ImGui::SmallButton` added on the same line as "Build Succeeded" text in the Build Output panel header, calls `openFolderInOS(buildConfig.buildDir)` — visible only when `buildState.exitCode == 0 && buildLog.lineCount() > 1`

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None.

## Self-Check: PASSED

- [x] `openFolderInOS` declared in `src/editor/build/EditorBuildSystem.h` (line 148)
- [x] `openFolderInOS` implemented in `src/editor/build/EditorBuildSystem.cpp` (line 695)
- [x] "Open Build Folder" menu item in `apps/level_editor/main.cpp` (line 777)
- [x] "Open Folder" SmallButton in `apps/level_editor/main.cpp` (line 2258)
- [x] `level-editor` target compiles cleanly
- [x] Commit 3b7addf exists
