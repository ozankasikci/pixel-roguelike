---
phase: quick
plan: 260408-uf2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/editor/scene/EditorSceneDocument.cpp
  - src/editor/scene/EditorPreviewWorld.cpp
autonomous: true
requirements: []
must_haves:
  truths:
    - "Door entities in the editor respect their scale property when rendering"
    - "Scaling a door via the gizmo persists the scale value back to LevelDoorPlacement"
    - "Editor preview world syncs door scale to TransformComponent for rendering"
  artifacts:
    - path: "src/editor/scene/EditorSceneDocument.cpp"
      provides: "localTransformMatrix uses p.scale for doors; applyWorldTransform stores scale back"
    - path: "src/editor/scene/EditorPreviewWorld.cpp"
      provides: "syncTransforms writes scale for DoorGroup case"
  key_links:
    - from: "EditorSceneDocument::localTransformMatrix"
      to: "LevelDoorPlacement::scale"
      via: "makeTransformMatrix third argument"
    - from: "EditorSceneDocument::applyWorldTransform"
      to: "LevelDoorPlacement::scale"
      via: "decomposed scale stored back to p.scale"
    - from: "EditorPreviewWorld::syncTransforms"
      to: "TransformComponent::scale"
      via: "DoorGroup case assigns transform.scale"
---

<objective>
Fix three bugs where door scale is ignored in the level editor. The LevelDoorPlacement struct
already has a `scale` field (LevelDef.h:116), but three code paths in the editor hardcode
scale to 1.0 or skip writing it, making gizmo scaling of doors non-functional.

Purpose: Enable doors to be scaled in the editor like every other entity type.
Output: Three surgical one-line fixes across two files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/editor/scene/EditorSceneDocument.cpp
@src/editor/scene/EditorPreviewWorld.cpp
@src/game/level/LevelDef.h (LevelDoorPlacement struct — has scale field at line 116)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix door scale in all three editor code paths</name>
  <files>src/editor/scene/EditorSceneDocument.cpp, src/editor/scene/EditorPreviewWorld.cpp</files>
  <action>
Apply three fixes:

**Fix 1 — EditorSceneDocument.cpp, localTransformMatrix (line 816):**
In the LevelDoorPlacement visitor branch, change the third argument of makeTransformMatrix
from `glm::vec3(1.0f)` to `p.scale`:
```cpp
return makeTransformMatrix(p.position, glm::vec3(0.0f, p.yawDegrees, 0.0f), p.scale);
```

**Fix 2 — EditorSceneDocument.cpp, applyWorldTransform (after line 610):**
In the LevelDoorPlacement visitor branch, after `p.yawDegrees = rotation.y;` add:
```cpp
p.scale = glm::max(scale, glm::vec3(0.01f));
```
This matches the LevelGroupNode pattern on line 603 and clamps to prevent zero/negative scale.

**Fix 3 — EditorPreviewWorld.cpp, syncTransforms, DoorGroup case (after line 468):**
After `transform.rotation.y = rotation.y;` add:
```cpp
transform.scale = scale;
```
This matches how ReflectionProbe and Mesh cases already sync scale.
  </action>
  <verify>
    <automated>cd /Users/ozan/Projects/gsd-3d-roguelike && cmake --build build --target pixel-roguelike level-editor 2>&1 | tail -5</automated>
  </verify>
  <done>
All three fixes applied. Build succeeds. Door scale flows through:
localTransformMatrix reads p.scale, applyWorldTransform writes p.scale back,
syncTransforms propagates scale to the preview TransformComponent.
  </done>
</task>

</tasks>

<verification>
1. Build succeeds with no errors or new warnings
2. Grep confirms no remaining `glm::vec3(1.0f)` in the LevelDoorPlacement branch of localTransformMatrix
3. Grep confirms `p.scale` is written in applyWorldTransform's LevelDoorPlacement branch
4. Grep confirms `transform.scale = scale` exists in the DoorGroup case of syncTransforms
</verification>

<success_criteria>
- Editor builds cleanly
- All three code paths now use door scale instead of hardcoded 1.0
- Door gizmo scaling in the editor is functional (scale persists and renders)
</success_criteria>

<output>
After completion, create `.planning/quick/260408-uf2-fix-door-scaling-in-editor-3-bugs-where-/260408-uf2-SUMMARY.md`
</output>
