---
phase: quick
plan: 260405-x13
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/core/PathUtils.cpp
autonomous: true
requirements: [ASSET-DISCOVERY]
must_haves:
  truths:
    - "Game finds assets/ when launched from any CWD with exe+assets as siblings"
    - "Game finds assets/ inside a macOS .app bundle (Contents/Resources/assets/)"
    - "PIXEL_ROGUELIKE_ROOT env var overrides all other search strategies"
    - "Diagnostic log lines show which search strategies were tried"
    - "Existing dev workflow (run from project root) still works unchanged"
  artifacts:
    - path: "src/engine/core/PathUtils.cpp"
      provides: "Updated projectRoot() with 5-strategy search order and diagnostic logging"
      contains: "PIXEL_ROGUELIKE_ROOT"
  key_links:
    - from: "src/engine/core/PathUtils.cpp"
      to: "spdlog"
      via: "spdlog::info/warn calls in projectRoot()"
      pattern: "spdlog::(info|warn)"
---

<objective>
Update `projectRoot()` in PathUtils.cpp to support five asset discovery strategies: env var override, macOS .app bundle detection, exe-relative walk-up, CWD walk-up, and CWD fallback. Add spdlog diagnostic logging so failures are diagnosable.

Purpose: The game currently fails to find assets when distributed outside the project tree (especially macOS .app bundles). This fix makes distribution work while preserving dev ergonomics.
Output: Updated PathUtils.cpp with robust, logged asset discovery.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/engine/core/PathUtils.cpp
@src/engine/core/PathUtils.h
</context>

<interfaces>
<!-- No interface changes. Public API stays the same: -->
From src/engine/core/PathUtils.h:
```cpp
std::string resolveProjectPath(const std::string& relativePath);
bool hasValidProjectRoot();
```
<!-- projectRoot() is file-local (anonymous namespace), all changes are internal. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite projectRoot() with 5-strategy search and diagnostic logging</name>
  <files>src/engine/core/PathUtils.cpp</files>
  <action>
Rewrite the `projectRoot()` function in the anonymous namespace. Keep `findProjectRoot()` unchanged. Keep `resolveProjectPath()` and `hasValidProjectRoot()` unchanged. The header (`PathUtils.h`) does NOT change.

Add `#include "spdlog/spdlog.h"` to the includes (after the existing ones, before project headers per convention). Also add `#include <cstdlib>` for `std::getenv`.

Replace the body of `projectRoot()` with this search order:

**Strategy 1 - Env var override (all platforms):**
```cpp
const char* envRoot = std::getenv("PIXEL_ROGUELIKE_ROOT");
if (envRoot) {
    fs::path envPath(envRoot);
    if (fs::exists(envPath / "assets")) {
        g_projectRoot = envPath.lexically_normal().string();
        spdlog::info("[PathUtils] Project root from PIXEL_ROGUELIKE_ROOT: {}", g_projectRoot);
        return g_projectRoot;
    }
    spdlog::warn("[PathUtils] PIXEL_ROGUELIKE_ROOT='{}' set but no assets/ found there", envRoot);
}
```

**Strategy 2 - macOS .app bundle detection (Apple only):**
Inside the existing `#if defined(__APPLE__)` block, BEFORE the exe walk-up:
- Get exe path via `_NSGetExecutablePath` (already there)
- Resolve symlinks with `fs::weakly_canonical()` on the path before using it
- Check if the resolved path string contains `.app/Contents/MacOS`
- If so, extract the bundle root (everything up to and including `.app`), construct `<bundle>/Contents/Resources/`, check if `assets/` exists there
- Use the string-parsing approach (NOT CoreFoundation) to avoid new framework dependencies
- Log: `spdlog::info("[PathUtils] Project root from macOS bundle: {}", ...)`

**Strategy 3 - Exe-relative walk-up (all platforms, existing logic):**
Keep the existing platform-specific exe path resolution (`_NSGetExecutablePath`, `GetModuleFileNameA`, `/proc/self/exe`). Apply `fs::weakly_canonical()` to the exe path on macOS before calling `findProjectRoot()`. Log on success: `spdlog::info("[PathUtils] Project root from exe walk-up: {}", ...)`.

**Strategy 4 - CWD walk-up (existing logic):**
This is currently strategy 1 in the code. MOVE it to run AFTER strategies 1-3 (after exe-relative). This changes the priority so exe-relative is checked before CWD. Wait -- per research decision, keep CWD-first for dev ergonomics. So actually:

REVISED order -- to preserve dev ergonomics as the research recommends:
1. Env var override
2. CWD walk-up (existing, stays first for dev ergonomics)
3. macOS .app bundle detection (new, Apple only)
4. Exe-relative walk-up (existing, all platforms -- apply weakly_canonical on macOS)
5. CWD fallback

Add `spdlog::info` on each success, `spdlog::warn` on the final fallback.

**Strategy 5 - Fallback to CWD:**
Keep existing fallback. Add: `spdlog::warn("[PathUtils] No assets/ found in any search path, falling back to CWD: {}", g_projectRoot)`.

IMPORTANT constraints:
- Do NOT add CoreFoundation dependency
- Do NOT change PathUtils.h (no public API changes)
- Do NOT change findProjectRoot() helper
- Do NOT change resolveProjectPath() or hasValidProjectRoot()
- Keep the `g_projectRoot` caching pattern (return early on non-empty)
  </action>
  <verify>
    <automated>cd /Users/ozan/Projects/gsd-3d-roguelike && cmake --build build --target pixel-roguelike 2>&1 | tail -5</automated>
  </verify>
  <done>
    - PathUtils.cpp compiles cleanly with no warnings
    - projectRoot() checks PIXEL_ROGUELIKE_ROOT env var first
    - CWD walk-up runs second (dev ergonomics preserved)
    - macOS .app bundle detection runs third (string-parsing, no CoreFoundation)
    - Exe-relative walk-up runs fourth (with weakly_canonical on macOS)
    - CWD fallback runs last with a warning log
    - Each strategy logs what it tried via spdlog::info/warn with "[PathUtils]" prefix
    - No header or public API changes
    - No new framework/library dependencies
  </done>
</task>

</tasks>

<verification>
- Build succeeds: `cmake --build build --target pixel-roguelike`
- Run the game from the project root and confirm it starts normally (assets found via CWD walk-up)
- Set `PIXEL_ROGUELIKE_ROOT` to the project root and run -- logs should show env var strategy used
- Set `PIXEL_ROGUELIKE_ROOT` to a nonexistent path -- logs should show warning, then fall through to other strategies
</verification>

<success_criteria>
- Game binary builds without errors or warnings in PathUtils.cpp
- Asset discovery works for: dev (CWD), flat distribution (exe-relative), macOS .app bundle, and env var override
- Diagnostic logging shows search path attempts on stdout/log
</success_criteria>

<output>
After completion, create `.planning/quick/260405-x13-fix-asset-discovery-so-game-works-when-d/260405-x13-SUMMARY.md`
</output>
