---
phase: quick
plan: 260330-wt0
type: execute
wave: 1
depends_on: []
files_modified:
  - src/editor/ui/EditorEnvironmentPanel.cpp
autonomous: true
requirements: [quick-env-panel-ux]
must_haves:
  truths:
    - "Post section controls are grouped by purpose (tone mapping, bloom, SSAO, fog, edges, color grading, effects, shadows)"
    - "Sky section controls are grouped by purpose (sky colors, sun, moon, panorama, clouds, horizon)"
    - "Enable checkboxes appear at the top of their related parameter group, not in a separate flat list"
    - "Groups use SeparatorText for lightweight visual headers within CollapsingHeader sections"
    - "Undo/redo tracking is preserved for every single control"
  artifacts:
    - path: "src/editor/ui/EditorEnvironmentPanel.cpp"
      provides: "Reorganized environment panel with grouped controls"
      contains: "SeparatorText"
  key_links:
    - from: "src/editor/ui/EditorEnvironmentPanel.cpp"
      to: "EditorSceneDocument"
      via: "trackEnvItem lambda + captureState/markEnvironmentDirty"
      pattern: "trackEnvItem.*captureState"
---

<objective>
Reorganize the Environment inspector panel for better UX by grouping related controls into logical sub-sections with visual separators.

Purpose: The Post section has 30+ controls in a flat list with no visual grouping. Checkboxes, sliders, and color pickers are interleaved without relationship to purpose. The Sky section similarly mixes sun, moon, panorama, cloud, and horizon controls at the same level.

Output: A reorganized EditorEnvironmentPanel.cpp with SeparatorText sub-groupings and enable-checkbox-at-top-of-group pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/editor/ui/EditorEnvironmentPanel.cpp
@src/editor/ui/LevelEditorUi.h (editColor, editVec3, editString signatures)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorganize Post section into logical sub-groups</name>
  <files>src/editor/ui/EditorEnvironmentPanel.cpp</files>
  <action>
Reorganize the Post CollapsingHeader section (lines 130-229) by grouping controls with `ImGui::SeparatorText("Group Name")`. The trackEnvItem lambda and beforeState capture pattern MUST remain identical for every control — only the ordering and separator insertion changes.

Groups (in this order within the Post section):

1. **SeparatorText("Tone Mapping")**
   - Enable Tone Map checkbox
   - Tone Mapper combo
   - Exposure, Gamma, Contrast, Saturation DragFloats

2. **SeparatorText("Bloom")**
   - Enable Bloom checkbox
   - Bloom Threshold, Bloom Intensity, Bloom Radius DragFloats

3. **SeparatorText("SSAO")**
   - Enable SSAO checkbox
   - AO Radius, AO Bias, AO Strength DragFloats

4. **SeparatorText("Fog")**
   - Enable Fog checkbox
   - Fog Density, Fog Start, Depth View Scale DragFloats
   - Fog Near Color, Fog Far Color editColors

5. **SeparatorText("Shadows")**
   - CSM Split Blend DragFloat (keep existing tooltip)

6. **SeparatorText("Edges")**
   - Enable Edges checkbox
   - Edge Threshold DragFloat

7. **SeparatorText("Color Grading")**
   - Split Tone Strength, Split Tone Balance DragFloats
   - Shadow Tint, Highlight Tint editColors

8. **SeparatorText("Effects")**
   - Enable Sky checkbox (this is a post toggle for sky compositing)
   - Enable Vignette checkbox + Vignette Strength, Vignette Softness
   - Enable Grain checkbox + Grain Amount
   - Enable Scanlines checkbox + Scanline Amount, Scanline Density
   - Enable Sharpen checkbox + Sharpen Amount

Within each group, place the enable checkbox FIRST, then its related parameters immediately after.

Do NOT change any control's label text, variable binding, drag speed, min, max, or format string.
Do NOT change the trackEnvItem lambda definition.
Do NOT change the trackEnvItem command label strings (the first string argument like "Toggle Bloom" etc.).
Do NOT remove or add any controls — same set, different order.
  </action>
  <verify>
    <automated>cd /Users/ozan/Projects/gsd-3d-roguelike && cmake --build build --target level-editor 2>&1 | tail -5</automated>
  </verify>
  <done>Post section has 8 SeparatorText groups with enable checkboxes at the top of each relevant group. All controls present, all undo/redo tracking preserved. Compiles without warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Reorganize Sky section into logical sub-groups</name>
  <files>src/editor/ui/EditorEnvironmentPanel.cpp</files>
  <action>
Reorganize the Sky CollapsingHeader section (lines 231-329) by grouping controls with `ImGui::SeparatorText("Group Name")` and collapsing the existing TreeNode sub-sections into the new grouping scheme.

Groups (in this order within the Sky section):

1. **Top-level** (no separator, first item under CollapsingHeader)
   - Sky Enabled checkbox
   - SeparatorText("Sky Colors")
   - Zenith, Horizon, Ground Haze editColors

2. **SeparatorText("Sun")**
   - Sun Direction editVec3
   - Sun Color editColor
   - Sun Size, Sun Glow DragFloats

3. **SeparatorText("Moon")**
   - Moon Enabled checkbox
   - Moon Direction editVec3
   - Moon Color editColor
   - Moon Size, Moon Glow DragFloats

4. **SeparatorText("Panorama")**
   - Panorama Path editString
   - Panorama Tint editColor
   - Panorama Strength, Panorama Yaw DragFloats

5. **Keep existing TreeNodeEx("Cubemap")** as-is (it already provides good grouping with its face paths + tint + strength). It goes after Panorama.

6. **SeparatorText("Clouds")**
   - Cloud Layer A editString (move from Sky Layers TreeNode)
   - Cloud Layer B editString (move from Sky Layers TreeNode)
   - Cloud Tint editColor
   - Cloud Scale, Cloud Speed, Cloud Coverage, Cloud Parallax DragFloats

7. **SeparatorText("Horizon")**
   - Horizon Layer editString (move from Sky Layers TreeNode)
   - Horizon Tint editColor
   - Horizon Height, Horizon Fade DragFloats

Remove the "Sky Layers" TreeNodeEx — its contents are distributed into Clouds and Horizon groups. Keep the "Cubemap" TreeNodeEx.

Same rules as Task 1: do NOT change any control labels, bindings, drag parameters, or trackEnvItem command strings. Only reorder and add SeparatorText.
  </action>
  <verify>
    <automated>cd /Users/ozan/Projects/gsd-3d-roguelike && cmake --build build --target level-editor 2>&1 | tail -5</automated>
  </verify>
  <done>Sky section has SeparatorText groups for Sky Colors, Sun, Moon, Panorama, Clouds, Horizon. Sky Layers TreeNode removed, its contents distributed. Cubemap TreeNode preserved. All controls present, undo/redo preserved. Compiles without warnings.</done>
</task>

</tasks>

<verification>
- Build the level-editor target: `cmake --build build --target level-editor`
- Launch the editor and open the Environment panel
- Confirm Post section shows 8 clearly labeled sub-groups with SeparatorText headers
- Confirm Sky section shows labeled sub-groups with Cubemap still in its TreeNode
- Confirm every checkbox, slider, color picker, and combo still works and triggers undo/redo
</verification>

<success_criteria>
- EditorEnvironmentPanel.cpp compiles without errors or warnings
- Post section: 30+ controls organized into 8 SeparatorText groups
- Sky section: controls organized into 6 SeparatorText groups + 1 TreeNode (Cubemap)
- Enable checkboxes appear at the top of their related parameter groups
- All undo/redo tracking works identically to before
- No controls added or removed — same set, reorganized
</success_criteria>

<output>
After completion, create `.planning/quick/260330-wt0-improve-environment-inspector-panel-ux-b/260330-wt0-SUMMARY.md`
</output>
