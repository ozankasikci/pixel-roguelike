---
type: quick
quick_id: 260330-wc2
description: Fix too-fast scrollbar scrolling in the editor asset browser panel
---

<objective>
Reduce the mouse wheel scroll speed in the Asset Browser panel. ImGui's default scroll step is 5 * font_size per notch, which is too fast for a tree view. Use ImGuiWindowFlags_NoScrollWithMouse to disable the default and re-implement with a reduced step (2 lines per notch).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add custom scroll speed to Asset Browser window</name>
  <files>
    src/editor/ui/EditorAssetBrowserPanel.cpp
  </files>
  <read_first>
    src/editor/ui/EditorAssetBrowserPanel.cpp
  </read_first>
  <action>
1. Change `ImGui::Begin("Asset Browser", open)` to `ImGui::Begin("Asset Browser", open, ImGuiWindowFlags_NoScrollWithMouse)` to disable default fast scrolling.

2. Immediately after the `ImGui::Begin` block (after line 284), add custom scroll handling:
```cpp
// Custom scroll speed — default 5 lines/notch is too fast for tree views
if (ImGui::IsWindowHovered(ImGuiHoveredFlags_RootAndChildWindows)) {
    float wheel = ImGui::GetIO().MouseWheel;
    if (wheel != 0.0f) {
        float scrollStep = ImGui::GetFontSize() * 2.0f;
        ImGui::SetScrollY(ImGui::GetScrollY() - wheel * scrollStep);
    }
}
```
  </action>
  <verify>
    <automated>cd /Users/ozan/Projects/gsd-3d-roguelike && cmake --build build --target level-editor 2>&1 | tail -5</automated>
  </verify>
  <done>Asset browser scrolls at 2 lines per notch instead of 5, editor compiles</done>
</task>

</tasks>
