---
phase: 02.1-equipment-inventory-and-carry-weight-weapons-are-inventory-items-no-slot-grid-inventory-dark-souls-style-carry-weight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/defs/weapons/old_dagger.weapon
  - assets/defs/weapons/brigand_axe.weapon
  - src/game/content/ContentRegistry.h
  - src/game/content/ContentRegistry.cpp
  - src/game/session/RunSession.h
  - src/game/session/EquipmentState.h
  - src/game/session/EquipmentState.cpp
  - tests/CMakeLists.txt
  - tests/test_content_registry.cpp
  - tests/test_equipment_state.cpp
autonomous: true
requirements: []

must_haves:
  truths:
    - "The run session can represent owned weapons independently from equipped left-hand and right-hand state per D-02, D-04, and D-05."
    - "A two-handed weapon occupies both hands and clears conflicting one-hand occupancy through one stable equipment rule per D-06 and the phase planning note."
    - "Equip burden is derived from effective equipped hands only, while owned but unequipped weapons contribute zero burden per D-08 and D-09."
    - "Weapon content exposes authored handedness, burden, category, and description so systems do not hardcode weapon ids."
  artifacts:
    - path: "src/game/session/EquipmentState.h"
      provides: "Stable equipment query and mutation interface for owned weapons, hand slots, and burden."
      min_lines: 40
      contains: "enum class EquipmentHand"
    - path: "src/game/session/RunSession.h"
      provides: "Run-scoped owned weapon inventory plus explicit equipped-hand state."
      min_lines: 20
      contains: "ownedWeapons"
    - path: "src/game/content/ContentRegistry.h"
      provides: "WeaponDefinition metadata needed by inventory and burden UI."
      min_lines: 40
      contains: "equipWeight"
    - path: "tests/test_equipment_state.cpp"
      provides: "Automated proof of hand occupancy and burden invariants."
      min_lines: 40
      contains: "two-handed"
  key_links:
    - from: "src/game/session/EquipmentState.cpp"
      to: "src/game/content/ContentRegistry.h"
      via: "WeaponDefinition handedness and equip weight lookups"
      pattern: "findWeapon|equipWeight|handedness"
    - from: "src/game/session/RunSession.h"
      to: "src/game/session/EquipmentState.h"
      via: "RunSession stores the owned/equipped data the helper operates on"
      pattern: "Equipment"
    - from: "tests/test_equipment_state.cpp"
      to: "src/game/session/EquipmentState.cpp"
      via: "Direct assertions over equip and burden rules"
      pattern: "equip|burden|two-handed"
---

<objective>
Define the weapon inventory/equipment data contracts and tests: authored weapon metadata, explicit owned-vs-equipped session state, and stable equipment queries that later gameplay systems can consume.

Purpose: This plan locks the rules of the feature before UI work starts, so the paused inventory screen and later combat/viewmodel work consume one consistent source of truth instead of inventing competing hand-state logic.
Output: Weapon metadata assets and parser support, run-session inventory/equipment state, equipment helper API, and automated tests for parsing and burden/equip invariants.
</objective>

<execution_context>
@/Users/ozan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ozan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-equipment-inventory-and-carry-weight-weapons-are-inventory-items-no-slot-grid-inventory-dark-souls-style-carry-weight/02.1-CONTEXT.md
@.planning/phases/02.1-equipment-inventory-and-carry-weight-weapons-are-inventory-items-no-slot-grid-inventory-dark-souls-style-carry-weight/02.1-RESEARCH.md
@.planning/phases/01.1-project-restructure-ecs-application-class-modular-engine-game-split/01.1-02-SUMMARY.md
@.planning/phases/01.1-project-restructure-ecs-application-class-modular-engine-game-split/01.1-03-SUMMARY.md
@src/game/session/RunSession.h
@src/game/content/ContentRegistry.h
@src/game/content/ContentRegistry.cpp
@tests/test_content_registry.cpp
@tests/CMakeLists.txt

<interfaces>
From src/game/session/RunSession.h:
```cpp
struct RunSession {
    struct LoadoutSlot {
        std::string definitionId;
        bool equipped = false;
    };

    std::string currentLevelId = "cathedral";
    glm::vec3 respawnPosition{0.0f, 1.6f, 5.4f};
    float fallRespawnY = -8.0f;
    std::vector<LoadoutSlot> loadout;
    std::unordered_set<std::string> unlockedSkillIds;
    std::unordered_set<std::string> collectedItemIds;
};
```

From src/game/content/ContentRegistry.h:
```cpp
struct WeaponDefinition {
    std::string id;
    std::string displayName;
    std::string slot;
    std::string meshId;
    float damage = 0.0f;
    float range = 0.0f;
    float cooldown = 0.0f;
};

class ContentRegistry {
public:
    void loadDefaults();
    const WeaponDefinition* findWeapon(const std::string& id) const;
};
```

From tests/test_content_registry.cpp:
```cpp
const auto weapon = loadWeaponDefinitionAsset(WEAPON_DEF_FILE);
assert(weapon.id == "old_dagger");
assert(weapon.slot == "melee");
assert(nearlyEqual(weapon.damage, 12.0f));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend weapon content metadata and parser coverage</name>
  <files>
    assets/defs/weapons/old_dagger.weapon,
    assets/defs/weapons/brigand_axe.weapon,
    src/game/content/ContentRegistry.h,
    src/game/content/ContentRegistry.cpp,
    tests/CMakeLists.txt,
    tests/test_content_registry.cpp
  </files>
  <read_first>
    .planning/phases/02.1-equipment-inventory-and-carry-weight-weapons-are-inventory-items-no-slot-grid-inventory-dark-souls-style-carry-weight/02.1-CONTEXT.md,
    .planning/phases/02.1-equipment-inventory-and-carry-weight-weapons-are-inventory-items-no-slot-grid-inventory-dark-souls-style-carry-weight/02.1-RESEARCH.md,
    src/game/content/ContentRegistry.h,
    src/game/content/ContentRegistry.cpp,
    assets/defs/weapons/old_dagger.weapon,
    tests/test_content_registry.cpp,
    tests/CMakeLists.txt
  </read_first>
  <behavior>
    - Test 1: `loadWeaponDefinitionAsset()` parses handedness, equip weight, category, and description for `old_dagger.weapon`.
    - Test 2: `ContentRegistry::loadDefaults()` loads both a one-handed weapon and a two-handed weapon so later plans can verify equip rules against real content.
    - Test 3: Parser continues honoring the repo's whitespace-tokenized asset format; new multi-word text stays underscore-safe rather than introducing a new parser format in this phase.
  </behavior>
  <action>
    Implement the authored weapon metadata required by D-01, D-02, D-06, D-08, D-12, and D-13 without creating a general inventory framework.

    In `src/game/content/ContentRegistry.h` and `src/game/content/ContentRegistry.cpp`, extend `WeaponDefinition` and the weapon parser with:
    - a handedness field with an explicit two-state invariant (`one_handed` or `two_handed`)
    - a numeric `equipWeight`
    - a minimal category label for list grouping
    - a short description string for the detail pane

    Keep the existing whitespace-tokenized asset format noted in research Pitfall 5. Do not introduce quoted strings or a new serialization format for this phase. Continue using underscores in authored text.

    Update `assets/defs/weapons/old_dagger.weapon` to include the new metadata, and add `assets/defs/weapons/brigand_axe.weapon` as the baseline two-handed test/starting weapon. `ContentRegistry::loadDefaults()` must load both weapons so the first playable inventory has real items and the later equipment tests can exercise both handedness paths.

    Update `tests/test_content_registry.cpp` and `tests/CMakeLists.txt` so the automated parsing coverage asserts the new metadata fields and proves both weapon assets are present in the registry. Avoid hardcoding UI assumptions or burden calculations here; keep this task focused on authored content contracts.
  </action>
  <verify>
    <automated>cmake -S /Users/ozan/Projects/gsd-3d-roguelike -B /Users/ozan/Projects/gsd-3d-roguelike/build -DPython_EXECUTABLE=/Users/ozan/Projects/gsd-3d-roguelike/.venv/bin/python3 && cmake --build /Users/ozan/Projects/gsd-3d-roguelike/build --target test_content_registry && ctest --test-dir /Users/ozan/Projects/gsd-3d-roguelike/build --output-on-failure -R '^content_registry$'</automated>
  </verify>
  <acceptance_criteria>
    - `WeaponDefinition` contains authored inventory metadata for handedness, equip weight, category, and description.
    - `ContentRegistry::loadDefaults()` loads `old_dagger` and `brigand_axe`.
    - `assets/defs/weapons/brigand_axe.weapon` exists and encodes a two-handed weapon using the same tokenized asset format as the existing defs.
    - `ctest -R '^content_registry$'` passes and asserts the new weapon metadata rather than only damage/range fields.
  </acceptance_criteria>
  <done>Weapon content definitions are rich enough to drive the paused inventory and burden UI without hardcoded weapon-id logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create explicit equipment state and burden query helpers</name>
  <files>
    src/game/session/RunSession.h,
    src/game/session/EquipmentState.h,
    src/game/session/EquipmentState.cpp,
    tests/CMakeLists.txt,
    tests/test_equipment_state.cpp
  </files>
  <read_first>
    .planning/phases/02.1-equipment-inventory-and-carry-weight-weapons-are-inventory-items-no-slot-grid-inventory-dark-souls-style-carry-weight/02.1-CONTEXT.md,
    .planning/phases/02.1-equipment-inventory-and-carry-weight-weapons-are-inventory-items-no-slot-grid-inventory-dark-souls-style-carry-weight/02.1-RESEARCH.md,
    src/game/session/RunSession.h,
    src/game/content/ContentRegistry.h,
    tests/CMakeLists.txt
  </read_first>
  <behavior>
    - Test 1: Owned weapon ids remain present in the session whether equipped or not, satisfying D-02.
    - Test 2: Equipping a two-handed weapon into either hand makes the effective left-hand and right-hand occupancy resolve to that same weapon, and burden counts it once.
    - Test 3: Equipping a one-handed weapon while a two-handed weapon is active clears the mirrored occupancy first, then equips only the requested hand.
    - Test 4: Burden is the sum of equipped effective weapons only; unequipped owned weapons add zero per D-08 and D-09.
  </behavior>
  <action>
    Replace the old boolean `loadout` model in `RunSession` with an explicit owned-vs-equipped structure that Phase 3 can query directly.

    Create `src/game/session/EquipmentState.h` and `src/game/session/EquipmentState.cpp` as the canonical equipment rule layer. Define:
    - explicit hand identifiers for right and left
    - a run-session-friendly owned weapon entry representation
    - explicit equipped-hand ids stored separately from owned entries
    - pure helper functions to equip, unequip, resolve effective occupancy, and compute equip burden from `WeaponDefinition`

    Lock in the phase rule from the planning notes: a two-handed weapon occupies both hands and clears conflicting one-hand occupancy immediately. Use one invariant consistently, not several special cases. A simple valid representation is that both hand ids point at the same two-handed weapon id, with helper logic preventing double-counted burden.

    Update `src/game/session/RunSession.h` to store:
    - owned weapon inventory seeded with the default starting weapons from Task 1
    - explicit right-hand and left-hand equipment state
    - a fixed max-equip-load value for the burden meter in this phase only; do not introduce stamina or stat scaling

    Add `tests/test_equipment_state.cpp` and wire it in `tests/CMakeLists.txt`. The tests must directly prove the two-handed displacement rule, the equipped-only burden rule, and the stable query surface that later combat/viewmodel code will consume. Do not let UI code, registry context, or ImGui types leak into this helper layer.
  </action>
  <verify>
    <automated>cmake -S /Users/ozan/Projects/gsd-3d-roguelike -B /Users/ozan/Projects/gsd-3d-roguelike/build -DPython_EXECUTABLE=/Users/ozan/Projects/gsd-3d-roguelike/.venv/bin/python3 && cmake --build /Users/ozan/Projects/gsd-3d-roguelike/build --target test_content_registry test_equipment_state && ctest --test-dir /Users/ozan/Projects/gsd-3d-roguelike/build --output-on-failure -R '^(content_registry|equipment_state)$'</automated>
  </verify>
  <acceptance_criteria>
    - `RunSession` stores owned weapons independently from explicit left-hand and right-hand equipment state.
    - `src/game/session/EquipmentState.h` exports stable functions that resolve effective equipped weapons and current burden without referencing ImGui or ECS UI state.
    - `tests/test_equipment_state.cpp` covers one-handed equip, two-handed equip, conflict clearing, and equipped-only burden.
    - `ctest -R '^(content_registry|equipment_state)$'` passes.
  </acceptance_criteria>
  <done>Owned weapon inventory, equipped-hand occupancy, and burden rules are explicit, test-backed, and ready for the paused inventory system to consume.</done>
</task>

</tasks>

<verification>
The phase slice is ready when weapon metadata loads from authored assets, `RunSession` no longer relies on the old boolean `loadout` flag model, and automated tests prove the two-handed and burden invariants without involving UI code.
</verification>

<success_criteria>
- Weapon definitions provide the metadata the paused inventory needs without hardcoded weapon-id branches.
- The session has one canonical representation for owned weapons and equipped hands.
- Two-handed occupancy and burden rules are locked in with automated tests before UI integration begins.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-equipment-inventory-and-carry-weight-weapons-are-inventory-items-no-slot-grid-inventory-dark-souls-style-carry-weight/02.1-01-SUMMARY.md`
</output>
