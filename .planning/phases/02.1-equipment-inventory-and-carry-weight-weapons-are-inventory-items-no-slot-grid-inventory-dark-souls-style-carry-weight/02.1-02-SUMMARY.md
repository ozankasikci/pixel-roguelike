---
phase: 02.1-equipment-inventory-and-carry-weight-weapons-are-inventory-items-no-slot-grid-inventory-dark-souls-style-carry-weight
plan: 02
subsystem: gameplay-ui
tags: [inventory, ui, input, pause, equipment]

# Dependency graph
requires:
  - phase: 02.1-equipment-inventory-and-carry-weight-weapons-are-inventory-items-no-slot-grid-inventory-dark-souls-style-carry-weight
    provides: Weapon metadata, owned-vs-equipped state, burden helpers, and equipment tests from Plan 01

provides:
  - A paused inventory menu state stored in registry context
  - An InventorySystem that opens on `I`, closes on `I`/`Escape`, and applies queued equip actions
  - A list-based ImGui inventory screen with category list, owned weapon list, details pane, and burden meter
  - Gameplay gating so movement, mouselook, and interaction pause while the inventory is open and resume after close
  - Localized inventory hotkey support that tolerates non-US keyboard layouts by matching key names and typed characters

affects: [phase-3-combat, pause-flow, equipment-ui]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Paused menu state lives in registry context and is read by gameplay systems as an additive pause signal
    - ImGui screens queue pending actions into state instead of mutating gameplay services directly
    - Resuming gameplay input after UI close should key off cursor lock state, not ImGui capture hints

key-files:
  created:
    - src/game/ui/InventoryMenuState.h
    - src/game/systems/InventorySystem.h
    - src/game/systems/InventorySystem.cpp
  modified:
    - src/engine/input/InputSystem.h
    - src/engine/input/InputSystem.cpp
    - src/engine/ui/ImGuiLayer.h
    - src/engine/ui/ImGuiLayer.cpp
    - src/game/systems/PlayerMovementSystem.cpp
    - src/game/systems/CameraSystem.cpp
    - src/game/systems/InteractionSystem.cpp
    - src/game/systems/RenderSystem.h
    - src/game/systems/RenderSystem.cpp
    - src/game/CMakeLists.txt
    - src/main.cpp

key-decisions:
  - "Inventory pause is separate from door/player interaction lock ownership"
  - "The inventory key accepts localized `i` variants instead of assuming a US layout"
  - "Closing the menu restores gameplay movement by returning to locked-cursor FPS mode"

patterns-established:
  - "One-shot UI actions should be written into menu state and consumed by a gameplay system on the next frame"
  - "Gameplay pause readers should depend on explicit feature state rather than ImGui capture flags"

requirements-completed: []

# Metrics
duration: 83min
completed: 2026-03-25
---

# Phase 02.1 Plan 02: Paused Inventory UI and Input Integration Summary

**A Dark Souls-style paused weapon inventory now opens from gameplay, renders through the existing ImGui path, and cleanly restores first-person controls when closed**

## Performance

- **Duration:** 83 min
- **Completed:** 2026-03-25
- **Tasks:** 2

## Accomplishments

- Added `InventoryMenuState` and `InventorySystem` to own paused inventory state, cursor transitions, and queued equip/unequip actions
- Integrated the inventory screen into the render path with a category column, owned weapon list, burden meter, and details pane
- Gated movement, camera look, and interaction while the inventory is open without reusing the shared door lock bool
- Fixed the inventory hotkey for localized keyboard layouts and fixed the post-close movement regression by restoring gameplay input from cursor-lock state
- Identified and removed confusion caused by a stale top-level `build/pixel-roguelike` binary; the current runtime target is `build/apps/runtime/pixel-roguelike`

## Verification

- PASSED: `ctest --test-dir build --output-on-failure -R '^equipment_state$'`
- PASSED: manual runtime verification that `I` opens inventory and closing it restores `WASD`

## Notes

- `AGENTS.md` was intentionally left uncommitted because it is local operator context, not product code.

---
