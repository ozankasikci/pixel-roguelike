---
phase: 21-feature-module-reorganization
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - apps/runtime/main.cpp
  - apps/level_editor/main.cpp
  - src/game/CMakeLists.txt
  - src/editor/CMakeLists.txt
  - CMakeLists.txt
  - src/game/components/DoorConfigComponent.h
  - src/game/components/DoorStateComponent.h
  - src/game/components/DoorLeafComponent.h
  - src/game/behavior/DoorAnimationSystem.h
  - src/game/behavior/DoorAnimationSystem.cpp
  - src/game/prefabs/GameplayPrefabs.h
  - src/game/prefabs/GameplayPrefabs.cpp
  - src/game/level/LevelBuilder.h
  - src/game/level/LevelBuilder.cpp
  - src/editor/ui/inspectors/DoorGroupInspector.h
  - src/editor/ui/inspectors/DoorGroupInspector.cpp
  - src/editor/ui/inspectors/SceneSelectionInspector.cpp
  - src/editor/scene/EditorPreviewWorld.cpp
  - src/game/runtime/RuntimeGameSession.cpp
  - src/game/level/LevelLoader.cpp
  - tests/game/CMakeLists.txt
  - tests/game/test_door_group_position.cpp
  - tests/game/test_level_roundtrip.cpp
  - tests/game/test_level_def.cpp
  - tests/game/test_runtime_game_session.cpp
autonomous: true
requirements: []

must_haves:
  truths:
    - "All door-related code lives in src/game/modules/door/ -- no door logic remains in components/, systems/, prefabs/, behavior/, or scattered editor files"
    - "Door spawning has a single implementation in DoorSpawner.cpp (not duplicated)"
    - "All existing tests pass with exit code 0"
    - "All three executables build and link successfully"
    - "registerDoorModule() is called at application startup in both runtime and editor"
    - "Old door files in their original locations are deleted"
  artifacts:
    - path: "apps/runtime/main.cpp"
      provides: "registerDoorModule() call at startup"
      contains: "registerDoorModule"
    - path: "apps/level_editor/main.cpp"
      provides: "registerDoorModule() call at startup"
      contains: "registerDoorModule"
  key_links:
    - from: "apps/runtime/main.cpp"
      to: "src/game/modules/door/DoorModule.h"
      via: "#include and function call"
      pattern: 'include.*game/modules/door/DoorModule'
    - from: "src/game/level/LevelLoader.cpp"
      to: "src/game/modules/door/DoorSpawner.h"
      via: "spawnDoorGroup call"
      pattern: "spawnDoorGroup"
    - from: "src/editor/ui/inspectors/SceneSelectionInspector.cpp"
      to: "src/game/modules/door/editor/DoorGroupInspector.h"
      via: "#include path update"
      pattern: 'include.*game/modules/door/editor/DoorGroupInspector'
---

<objective>
Wire registerDoorModule() at application startup, update all include paths to point at the module, remove old door files from their original locations, update CMake targets, move and update tests, and verify everything builds and passes.

Purpose: This is the final cutover -- the door module becomes the sole owner of all door code. Old files are deleted, include paths updated, CMake targets updated, and tests wired. After this plan, the codebase demonstrates the folder-per-feature pattern with the door module as proof of concept.

Output: Clean build with all door code in src/game/modules/door/, old files deleted, all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-feature-module-reorganization-folder-per-feature-structure-with-door-module-as-proof-of-concept/21-CONTEXT.md
@.planning/phases/21-feature-module-reorganization-folder-per-feature-structure-with-door-module-as-proof-of-concept/21-RESEARCH.md
@.planning/phases/21-feature-module-reorganization-folder-per-feature-structure-with-door-module-as-proof-of-concept/21-01-SUMMARY.md
@.planning/phases/21-feature-module-reorganization-folder-per-feature-structure-with-door-module-as-proof-of-concept/21-02-SUMMARY.md

<interfaces>
<!-- After Plan 01 + 02, these are the module's public API -->

From src/game/modules/door/DoorModule.h:
```cpp
void registerDoorModule();
```

From src/game/modules/door/DoorSpawner.h:
```cpp
entt::entity spawnDoorGroup(LevelBuilder& builder,
                             const LevelDoorPlacement& group,
                             const LevelDef& level);
```

From src/game/modules/door/DoorComponents.h:
```cpp
struct DoorConfigComponent { ... };
struct DoorStateComponent { ... };
struct DoorLeafComponent { ... };
bool isDoorFullyClosed(const DoorStateComponent& s);
bool isDoorFullyOpen(const DoorStateComponent& s);
bool isDoorMoving(const DoorStateComponent& s);
```

From src/game/modules/door/DoorAnimationSystem.h:
```cpp
class DoorAnimationSystem : public System { ... };
void tickDoorAnimation(entt::registry& registry, float deltaTime);
```

From src/game/modules/door/DoorMath.h:
```cpp
glm::mat4 makePivotLeafModel(...);
```

From src/game/modules/door/editor/DoorGroupInspector.h:
```cpp
void drawDoorGroupInspector(...);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire registerDoorModule at startup, update include paths, switch to DoorSpawner</name>
  <files>
    apps/runtime/main.cpp
    apps/level_editor/main.cpp
    src/game/level/LevelBuilder.h
    src/game/level/LevelBuilder.cpp
    src/game/level/LevelLoader.cpp
    src/game/runtime/RuntimeGameSession.cpp
    src/editor/scene/EditorPreviewWorld.cpp
    src/editor/ui/inspectors/SceneSelectionInspector.cpp
  </files>
  <read_first>
    apps/runtime/main.cpp
    apps/level_editor/main.cpp
    src/game/level/LevelBuilder.h
    src/game/level/LevelBuilder.cpp
    src/game/level/LevelLoader.cpp
    src/game/runtime/RuntimeGameSession.cpp
    src/editor/scene/EditorPreviewWorld.cpp
    src/editor/ui/inspectors/SceneSelectionInspector.cpp
    src/game/modules/door/DoorModule.h
    src/game/modules/door/DoorSpawner.h
    src/game/modules/door/DoorComponents.h
    src/game/modules/door/DoorAnimationSystem.h
    src/game/modules/door/editor/DoorGroupInspector.h
  </read_first>
  <action>
**apps/runtime/main.cpp** — Add `#include "game/modules/door/DoorModule.h"` to the includes. Call `registerDoorModule();` early in main(), BEFORE any scene loading. Place it after `content.loadDefaults()` and before `SceneManager sceneManager;` (around line 86). Also update the DoorAnimationSystem include from `"game/behavior/DoorAnimationSystem.h"` to `"game/modules/door/DoorAnimationSystem.h"`.

**apps/level_editor/main.cpp** — Add `#include "game/modules/door/DoorModule.h"` to the includes. Call `registerDoorModule();` early in the editor main(), BEFORE any scene loading (before the main editor loop starts). The exact placement: after content registry initialization, before the first frame. Also check if this file includes DoorAnimationSystem -- if so, update to module path.

**src/game/level/LevelBuilder.h** — Remove the `addDoorGroup` method declaration (line 60: `entt::entity addDoorGroup(const LevelDoorPlacement& group, const LevelDef& level);`). The `#include "game/level/LevelDef.h"` stays because LevelBuilder uses other LevelDef types. Remove any door-specific forward declarations if present.

**src/game/level/LevelBuilder.cpp** — Delete the entire `LevelBuilder::addDoorGroup()` method (lines 283-399). Remove the following includes that were only needed for addDoorGroup:
- `#include "game/components/DoorConfigComponent.h"` (verify no other usage first)
- `#include "game/components/DoorStateComponent.h"` (same)
- `#include "game/components/DoorLeafComponent.h"` (same)
- `#include "game/prefabs/GameplayPrefabs.h"` (ONLY if makePivotLeafModel was the sole usage -- check for spawnCheckpoint/spawnGameplayPrefab usage, which are still used by LevelLoader)

Actually, LevelLoader.cpp includes GameplayPrefabs.h for `spawnGameplayPrefab` and `spawnCheckpoint`. So `GameplayPrefabs.h` keeps its non-door functions. Only remove the `makePivotLeafModel` declaration from GameplayPrefabs.h later (in Task 2).

For LevelBuilder.cpp: remove the `#include "game/components/DoorConfigComponent.h"`, `DoorStateComponent.h`, `DoorLeafComponent.h"` includes. Keep `GameplayPrefabs.h` only if other functions from it are used in LevelBuilder.cpp (check -- likely not, since LevelLoader calls prefabs directly). Remove `#include "game/components/MeshComponent.h"` only if it's not used elsewhere in LevelBuilder.cpp (it is -- keep it).

Remove `#include "game/behavior/ActionTypes.h"` and `#include "game/behavior/BehaviorComponent.h"` from LevelBuilder.cpp ONLY IF no other code in the file references them. The `addCollider` and `attachBehaviors` methods still use BehaviorComponent/ActionTypes -- keep those includes.

**src/game/level/LevelLoader.cpp** — Replace `builder.addDoorGroup(doorGroup, level);` (line 90) with:

```cpp
spawnDoorGroup(builder, doorGroup, level);
```

Add `#include "game/modules/door/DoorSpawner.h"` to the includes.

The door child mesh skipping logic (lines 56-69 with `doorChildNodeIds`) stays in LevelLoader.cpp since it's about the loading orchestration, not door spawning itself.

**src/game/runtime/RuntimeGameSession.cpp** — Update include paths:
- `#include "game/components/DoorConfigComponent.h"` -> `#include "game/modules/door/DoorComponents.h"`
- `#include "game/components/DoorStateComponent.h"` -> remove (already covered by DoorComponents.h)
- `#include "game/behavior/DoorAnimationSystem.h"` -> `#include "game/modules/door/DoorAnimationSystem.h"`

All runtime code in RuntimeGameSession.cpp that uses DoorConfigComponent, DoorStateComponent, or tickDoorAnimation will now resolve from the module headers. No code changes needed -- just include path updates.

**src/editor/scene/EditorPreviewWorld.cpp** — Update include paths:
- `#include "game/components/DoorConfigComponent.h"` -> `#include "game/modules/door/DoorComponents.h"`
- `#include "game/components/DoorStateComponent.h"` -> remove (covered by DoorComponents.h)

The DoorGroup case at line 230 stays as-is (it's the intentionally simplified editor preview path per research -- just update includes).

**src/editor/ui/inspectors/SceneSelectionInspector.cpp** — Update include path:
- `#include "editor/ui/inspectors/DoorGroupInspector.h"` -> `#include "game/modules/door/editor/DoorGroupInspector.h"`
  </action>
  <verify>
    <automated>cd /Users/ozan/Projects/gsd-3d-roguelike && cmake --build build --target pixel-roguelike level-editor 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/runtime/main.cpp contains `#include "game/modules/door/DoorModule.h"` and `registerDoorModule()`
    - apps/level_editor/main.cpp contains `#include "game/modules/door/DoorModule.h"` and `registerDoorModule()`
    - src/game/level/LevelBuilder.h does NOT contain `addDoorGroup`
    - src/game/level/LevelBuilder.cpp does NOT contain `LevelBuilder::addDoorGroup`
    - src/game/level/LevelLoader.cpp contains `spawnDoorGroup(builder,` instead of `builder.addDoorGroup(`
    - src/game/runtime/RuntimeGameSession.cpp contains `#include "game/modules/door/DoorComponents.h"`
    - src/game/runtime/RuntimeGameSession.cpp contains `#include "game/modules/door/DoorAnimationSystem.h"`
    - src/editor/ui/inspectors/SceneSelectionInspector.cpp contains `#include "game/modules/door/editor/DoorGroupInspector.h"`
    - `cmake --build build --target pixel-roguelike` succeeds
    - `cmake --build build --target level-editor` succeeds
  </acceptance_criteria>
  <done>registerDoorModule() called at startup in both executables, all include paths point to module, LevelBuilder.addDoorGroup removed, LevelLoader uses spawnDoorGroup, all executables build</done>
</task>

<task type="auto">
  <name>Task 2: Delete old door files, update CMake targets, update tests, verify full build + test pass</name>
  <files>
    src/game/components/DoorConfigComponent.h
    src/game/components/DoorStateComponent.h
    src/game/components/DoorLeafComponent.h
    src/game/behavior/DoorAnimationSystem.h
    src/game/behavior/DoorAnimationSystem.cpp
    src/game/prefabs/GameplayPrefabs.h
    src/game/prefabs/GameplayPrefabs.cpp
    src/editor/ui/inspectors/DoorGroupInspector.h
    src/editor/ui/inspectors/DoorGroupInspector.cpp
    src/game/CMakeLists.txt
    src/editor/CMakeLists.txt
    CMakeLists.txt
    tests/game/CMakeLists.txt
    tests/game/test_door_group_position.cpp
    tests/game/test_level_roundtrip.cpp
    tests/game/test_level_def.cpp
    tests/game/test_runtime_game_session.cpp
  </files>
  <read_first>
    src/game/CMakeLists.txt
    src/editor/CMakeLists.txt
    CMakeLists.txt
    tests/game/CMakeLists.txt
    tests/game/test_door_group_position.cpp
    tests/game/test_level_roundtrip.cpp
    tests/game/test_level_def.cpp
    tests/game/test_runtime_game_session.cpp
    src/game/prefabs/GameplayPrefabs.h
    src/game/prefabs/GameplayPrefabs.cpp
    src/game/behavior/ActionTypes.h
  </read_first>
  <action>
**Delete old door files** — Remove these files completely (git rm):

```bash
git rm src/game/components/DoorConfigComponent.h
git rm src/game/components/DoorStateComponent.h
git rm src/game/components/DoorLeafComponent.h
git rm src/game/behavior/DoorAnimationSystem.h
git rm src/game/behavior/DoorAnimationSystem.cpp
git rm src/editor/ui/inspectors/DoorGroupInspector.h
git rm src/editor/ui/inspectors/DoorGroupInspector.cpp
```

**Update GameplayPrefabs** — Remove `makePivotLeafModel()` from GameplayPrefabs:
- In `GameplayPrefabs.h`: Remove the `makePivotLeafModel` declaration (lines 19-24) and its doc comment (lines 10-18). Keep `spawnCheckpoint()` and `spawnGameplayPrefab()` declarations.
- In `GameplayPrefabs.cpp`: Remove the `makePivotLeafModel` function body. Keep `spawnCheckpoint()` and `spawnGameplayPrefab()` implementations. Remove any includes that were only needed for makePivotLeafModel (`<glm/gtc/matrix_transform.hpp>`, `<cmath>`) IF not used by remaining functions. Check carefully before removing.

**Update ActionTypes.h** — Per D-08, move DoorActionParams out. BUT per research Open Question 2, keep the ActionType enum values (OpenDoor/CloseDoor/ToggleDoor) in ActionTypes.h since they are shared interface. Remove:
- The `struct DoorActionParams { float duration = 1.2f; };` definition (line 27-28)
- Replace `DoorActionParams` in the `ActionParams` variant with a forward-include approach:
  - Add `#include "game/modules/door/DoorActionTypes.h"` to ActionTypes.h to retain the variant member
  - This is a small layer "violation" (core header includes module header) but it keeps the ActionParams variant correct without breaking API

If adding this include creates a circular dependency (check: DoorActionTypes.h should NOT include ActionTypes.h), it should be fine since DoorActionTypes.h only defines the struct with no includes besides `<cstdint>`.

**Update src/game/CMakeLists.txt** — In the `gameplay` target source list, remove:
- `behavior/DoorAnimationSystem.cpp` (moved to module)

The `game_module_door` is already added via `add_subdirectory(modules/door)` from Plan 01.

**Update src/editor/CMakeLists.txt** — Two changes:
1. Remove `ui/inspectors/DoorGroupInspector.cpp` from the `editor` target source list (old location)
2. Add `${CMAKE_SOURCE_DIR}/src/game/modules/door/editor/DoorGroupInspector.cpp` to the `editor` target source list (new location, per D-02 and D-04)
3. Add `game_module_door` to `target_link_libraries(editor PUBLIC ...)` alongside `gameplay` -- the editor needs to link the module for DoorComponents types used by EditorPreviewWorld

**Update CMakeLists.txt (root)** — Check if the root CMakeLists.txt or `apps/` CMakeLists need `game_module_door` added to link targets:
- `pixel-roguelike` executable: needs `game_module_door` because main.cpp calls `registerDoorModule()` and the game links door-dependent code
- `level-editor` executable: needs `game_module_door` for same reason
- Check how executables are linked -- if they link `game` (the INTERFACE library) or `gameplay` directly. Since `game_module_door` depends on `gameplay` but `gameplay` does NOT depend on `game_module_door`, executables must explicitly link `game_module_door`.

Add `game_module_door` to the executable link targets. Look for `target_link_libraries(pixel-roguelike ...)` and `target_link_libraries(level-editor ...)` in either root CMakeLists.txt or apps/*/CMakeLists.txt. Add `game_module_door` to both.

**Update tests** — 

For `test_door_group_position.cpp`:
- Update `#include "game/prefabs/GameplayPrefabs.h"` to `#include "game/modules/door/DoorMath.h"` (for makePivotLeafModel)
- The test also uses `LevelDef.h` which stays unchanged

For `test_level_roundtrip.cpp`:
- This test exercises the door_group round-trip via parser. After the registration refactor, the parser only works if `registerDoorModule()` is called first. Add `#include "game/modules/door/DoorModule.h"` and call `registerDoorModule();` at the start of main() BEFORE any `loadLevelDef()` calls.

For `test_level_def.cpp`:
- Similar: if this test calls `loadLevelDef()` on a scene file that contains `door_group` records, it needs `registerDoorModule()` first. Add the include and registration call.

For `test_runtime_game_session.cpp`:
- Check if it loads a scene with door_group records. If so, add `registerDoorModule()` before the session build. Also check for door component includes and update paths if needed.

For `test_behavior_trigger_roundtrip.cpp`:
- Check if it references door action types. If so, add the registration call.

**Update tests/game/CMakeLists.txt** — For each test that now needs `game_module_door`:
- `test_door_group_position`: Change `LIBRARIES gameplay` to `LIBRARIES gameplay game_module_door`
- `test_level_roundtrip`: Change `LIBRARIES game_content` to `LIBRARIES game_content game_module_door`
- `test_level_def`: Change `LIBRARIES game_content` to `LIBRARIES game_content game_module_door`
- `test_runtime_game_session`: Change `LIBRARIES gameplay` to `LIBRARIES gameplay game_module_door`
- Check `test_behavior_trigger_roundtrip`: if it needs door module, add the link

**Final build + test verification:**
```bash
cmake -B build -DCMAKE_BUILD_TYPE=Debug
cmake --build build
cd build && ctest --output-on-failure -L game
```
  </action>
  <verify>
    <automated>cd /Users/ozan/Projects/gsd-3d-roguelike && cmake --build build 2>&1 | tail -5 && cd build && ctest --output-on-failure -L game 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/game/components/DoorConfigComponent.h does NOT exist (deleted)
    - src/game/components/DoorStateComponent.h does NOT exist (deleted)
    - src/game/components/DoorLeafComponent.h does NOT exist (deleted)
    - src/game/behavior/DoorAnimationSystem.h does NOT exist (deleted)
    - src/game/behavior/DoorAnimationSystem.cpp does NOT exist (deleted)
    - src/editor/ui/inspectors/DoorGroupInspector.h does NOT exist (deleted)
    - src/editor/ui/inspectors/DoorGroupInspector.cpp does NOT exist (deleted)
    - src/game/prefabs/GameplayPrefabs.h does NOT contain `makePivotLeafModel`
    - src/game/CMakeLists.txt does NOT contain `DoorAnimationSystem.cpp` in gameplay sources
    - src/editor/CMakeLists.txt contains `game/modules/door/editor/DoorGroupInspector.cpp`
    - src/game/behavior/ActionTypes.h does NOT contain `struct DoorActionParams` (moved to module)
    - src/game/behavior/ActionTypes.h contains `#include "game/modules/door/DoorActionTypes.h"`
    - tests/game/test_door_group_position.cpp contains `#include "game/modules/door/DoorMath.h"`
    - tests/game/test_level_roundtrip.cpp contains `registerDoorModule()`
    - `cmake --build build` succeeds (all three executables)
    - `ctest --output-on-failure -L game` shows all tests passing
  </acceptance_criteria>
  <done>All old door files deleted, CMake targets updated, tests updated and passing, all three executables build, the door module is the sole owner of all door code</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Module registration ordering | registerDoorModule must be called before loadLevelDef |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | D (DoS) | Missing registration | mitigate | registerDoorModule() called early in main(), before any scene loading; test_level_roundtrip.cpp also calls it before loadLevelDef (per research Pitfall 3) |
| T-21-06 | T (Tampering) | Include path ActionTypes.h -> module | accept | Transitional include of DoorActionTypes.h from ActionTypes.h; low risk, enables clean variant type |
</threat_model>

<verification>
- `cmake --build build` succeeds for all targets (pixel-roguelike, level-editor, procedural-model-viewer)
- `ctest --output-on-failure -L game` -- all tests pass
- No file with "Door" in its name exists under src/game/components/, src/game/behavior/ (for DoorAnimationSystem), src/editor/ui/inspectors/ (for DoorGroupInspector)
- All door code is under src/game/modules/door/
- grep -r "DoorConfigComponent" src/game/components/ returns no results
- grep -r "DoorAnimationSystem" src/game/behavior/ returns no results (header/cpp deleted)
</verification>

<success_criteria>
All door code consolidated in src/game/modules/door/. Old files deleted. All three executables build. All tests pass. The feature-module pattern is proven: opening src/game/modules/door/ shows the entire door feature; hypothetically deleting the folder (and un-registering) would cleanly remove the feature.
</success_criteria>

<output>
After completion, create `.planning/phases/21-feature-module-reorganization-folder-per-feature-structure-with-door-module-as-proof-of-concept/21-03-SUMMARY.md`
</output>
