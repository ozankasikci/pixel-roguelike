---
phase: 15-prototype-editor-imgui-themes-and-choose-a-final-skin-direction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/ui/ImGuiLayer.h
  - src/engine/ui/ImGuiLayer.cpp
  - apps/level_editor/main.cpp
autonomous: true
requirements: []

must_haves:
  truths:
    - "The editor exposes exactly three named interface themes: Warm Studio Dark, Spectrum-Inspired Dark, and Soft Light Tooling"
    - "Switching the interface theme from the View menu updates the whole editor chrome without restarting"
    - "The three presets feel visually distinct in-editor: Warm Studio Dark reads as warm dark chrome, Spectrum-Inspired Dark reads as a cooler professional dark preset, and Soft Light Tooling reads as a lighter paper-like preset"
    - "Changing presets keeps the 3D viewport readable and visually subordinate while menus, panels, tabs, popups, and buttons restyle together"
  artifacts:
    - path: "src/engine/ui/ImGuiLayer.h"
      provides: "Theme preset enum, labels, state accessors, and request API"
      contains: "enum class ImGuiThemePreset"
    - path: "src/engine/ui/ImGuiLayer.cpp"
      provides: "Three repo-local theme preset functions and pre-NewFrame application path"
      contains: "applyPendingThemePreset"
    - path: "apps/level_editor/main.cpp"
      provides: "View -> Interface Theme live selector in the editor menu bar"
      contains: "Interface Theme"
  key_links:
    - from: "apps/level_editor/main.cpp"
      to: "src/engine/ui/ImGuiLayer.h"
      via: "View menu item -> requestThemePreset"
      pattern: "requestThemePreset"
    - from: "src/engine/ui/ImGuiLayer.cpp"
      to: "Dear ImGui frame lifecycle"
      via: "applyPendingThemePreset before ImGui::NewFrame"
      pattern: "applyPendingThemePreset\\(\\).*ImGui::NewFrame"
    - from: "src/engine/ui/ImGuiLayer.cpp"
      to: "upstream style baselines"
      via: "StyleColorsDark/StyleColorsLight + repo-local delta"
      pattern: "StyleColors(Dark|Light)"
---

<objective>
Add the theme-preset foundation for the editor UI: three repo-local Dear ImGui presets implemented in `ImGuiLayer`, plus a live `View -> Interface Theme` selector for instant comparison.

Purpose: Phase 15 is about converging on a warmer, more polished editor chrome without inventing a theming framework or forking Dear ImGui. This plan establishes the lowest-risk theme architecture and the live comparison workflow.

Output: `ImGuiThemePreset` support in `ImGuiLayer`, concrete preset functions for the three approved directions, and a menu-driven theme switcher in the level editor.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-prototype-editor-imgui-themes-and-choose-a-final-skin-direction/15-CONTEXT.md
@.planning/phases/15-prototype-editor-imgui-themes-and-choose-a-final-skin-direction/15-RESEARCH.md
@src/engine/ui/ImGuiLayer.h
@src/engine/ui/ImGuiLayer.cpp
@apps/level_editor/main.cpp

<interfaces>
From src/engine/ui/ImGuiLayer.h:
```cpp
enum class ImGuiFontPreset {
    SystemSans,
    Verdana,
    AvenirNext,
    HelveticaNeue,
    TrebuchetMS,
    InterUnity,
    RobotoUnreal,
    JetBrainsMonoGodot,
};

const char* imguiFontPresetLabel(ImGuiFontPreset preset);

class ImGuiLayer {
public:
    void requestFontPreset(ImGuiFontPreset preset);
    ImGuiFontPreset fontPreset() const { return fontPreset_; }
    void beginFrame();
    // ...
};
```

From apps/level_editor/main.cpp:
```cpp
ImGuiLayer imgui;
imgui.init(window.handle());

ImGuiFontPreset editorFontPreset = imgui.fontPreset();

if (ImGui::BeginMenu("View")) {
    if (ImGui::BeginMenu("Interface Font")) {
        // existing live preset menu pattern
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repo-local ImGui theme presets and pre-frame theme application to ImGuiLayer</name>
  <files>
    src/engine/ui/ImGuiLayer.h
    src/engine/ui/ImGuiLayer.cpp
  </files>
  <read_first>
    .planning/PROJECT.md
    .planning/phases/15-prototype-editor-imgui-themes-and-choose-a-final-skin-direction/15-CONTEXT.md
    .planning/phases/15-prototype-editor-imgui-themes-and-choose-a-final-skin-direction/15-RESEARCH.md
    src/engine/ui/ImGuiLayer.h
    src/engine/ui/ImGuiLayer.cpp
  </read_first>
  <action>
Extend `ImGuiLayer` with a lightweight theme-preset API per D-01, D-02, D-03, and D-04. Do not add a skinning framework, do not introduce custom widget rendering, and do not touch runtime/editor panel code in this task.

Preset intent and scope constraints that must be reflected directly in the implementation:
- Per D-06, `Spectrum-Inspired Dark` is a professional-tools reference point only. Use it to capture calmer hierarchy and clearer active/hover states, but do not try to mirror Adobe Spectrum token-for-token or import an external Spectrum skin.
- Per D-07, `Soft Light Tooling` exists as a contrast check for whether a lighter editor is preferable. It should feel intentional and paper-like, but it is not the expected default and should not be tuned as the primary winner.
- Per D-08, style only the editor chrome surfaces that drive readability every day: window backgrounds, panels, headers, menus, tabs, buttons, separators, borders, selection accents, rounding, spacing, and frame padding. Do not expand this task into custom-drawn widgets or unrelated UI redesign.
- Per D-10, the viewport must remain visually subordinate to the 3D scene. Keep the chrome readable around it, but do not add bright accent blocks, heavy borders, or high-energy fills that fight the scene content.
- Per D-14, D-15, and D-16, all three presets must stay warm or restrained rather than neon/gamer-styled, favor readable contrast and hierarchy over novelty, and specifically avoid the colder blue/purple stock-ImGui feel for the default dark direction.

1. In `src/engine/ui/ImGuiLayer.h`, add:
   ```cpp
   enum class ImGuiThemePreset {
       WarmStudioDark,
       SpectrumInspiredDark,
       SoftLightTooling,
   };

   const char* imguiThemePresetLabel(ImGuiThemePreset preset);
   ```
   plus these class methods and members:
   ```cpp
   void requestThemePreset(ImGuiThemePreset preset);
   ImGuiThemePreset themePreset() const { return themePreset_; }

   void applyPendingThemePreset();

   ImGuiThemePreset themePreset_ = ImGuiThemePreset::WarmStudioDark;
   std::optional<ImGuiThemePreset> pendingThemePreset_ = ImGuiThemePreset::WarmStudioDark;
   ```

2. In `src/engine/ui/ImGuiLayer.cpp`, add three file-local preset functions that mutate `ImGuiStyle& style` after calling an upstream base style:
   - `applyWarmStudioDark(ImGuiStyle& style)` must start from `ImGui::StyleColorsDark(&style)`
   - `applySpectrumInspiredDark(ImGuiStyle& style)` must start from `ImGui::StyleColorsDark(&style)`
   - `applySoftLightTooling(ImGuiStyle& style)` must start from `ImGui::StyleColorsLight(&style)`

3. Use these exact layout metrics unless a field does not exist in the current Dear ImGui version:
   - `Warm Studio Dark`: `WindowRounding=6.0f`, `FrameRounding=5.0f`, `GrabRounding=5.0f`, `TabRounding=5.0f`, `ScrollbarRounding=6.0f`, `WindowBorderSize=1.0f`, `PopupBorderSize=1.0f`, `FrameBorderSize=1.0f`, `WindowPadding=(10, 8)`, `FramePadding=(8, 6)`, `ItemSpacing=(8, 6)`, `CellPadding=(8, 6)`
   - `Spectrum-Inspired Dark`: `WindowRounding=4.0f`, `FrameRounding=4.0f`, `GrabRounding=4.0f`, `TabRounding=4.0f`, `ScrollbarRounding=5.0f`, `WindowBorderSize=1.0f`, `PopupBorderSize=1.0f`, `FrameBorderSize=1.0f`, `WindowPadding=(9, 8)`, `FramePadding=(7, 5)`, `ItemSpacing=(8, 5)`, `CellPadding=(7, 5)`
   - `Soft Light Tooling`: `WindowRounding=6.0f`, `FrameRounding=5.0f`, `GrabRounding=5.0f`, `TabRounding=5.0f`, `ScrollbarRounding=6.0f`, `WindowBorderSize=1.0f`, `PopupBorderSize=1.0f`, `FrameBorderSize=1.0f`, `WindowPadding=(10, 8)`, `FramePadding=(8, 6)`, `ItemSpacing=(8, 6)`, `CellPadding=(8, 6)`

4. Apply these exact color anchors to the relevant `ImGuiCol_*` entries so the preset intent is concrete:
   - `WarmStudioDark`
     - `Text = (0.90, 0.87, 0.81, 1.00)`
     - `WindowBg = (0.12, 0.11, 0.10, 0.98)`
     - `ChildBg = (0.14, 0.13, 0.12, 0.98)`
     - `PopupBg = (0.15, 0.13, 0.11, 0.98)`
     - `MenuBarBg = (0.16, 0.14, 0.12, 1.00)`
     - `Border = (0.34, 0.29, 0.22, 0.65)`
     - `FrameBg = (0.19, 0.17, 0.15, 1.00)`
     - `FrameBgHovered = (0.27, 0.23, 0.18, 1.00)`
     - `FrameBgActive = (0.31, 0.26, 0.20, 1.00)`
     - `Button = (0.30, 0.25, 0.19, 1.00)`
     - `ButtonHovered = (0.63, 0.52, 0.31, 1.00)`
     - `ButtonActive = (0.54, 0.44, 0.24, 1.00)`
     - `Header = (0.29, 0.24, 0.18, 0.85)`
     - `HeaderHovered = (0.38, 0.31, 0.21, 0.92)`
     - `HeaderActive = (0.54, 0.44, 0.24, 1.00)`
     - `Tab = (0.20, 0.17, 0.14, 1.00)`
     - `TabHovered = (0.38, 0.31, 0.21, 0.92)`
     - `TabSelected = (0.28, 0.24, 0.18, 1.00)`
   - `SpectrumInspiredDark`
     - `Text = (0.92, 0.94, 0.96, 1.00)`
     - `WindowBg = (0.10, 0.11, 0.12, 0.98)`
     - `ChildBg = (0.12, 0.13, 0.15, 0.98)`
     - `PopupBg = (0.13, 0.14, 0.16, 0.98)`
     - `MenuBarBg = (0.14, 0.15, 0.17, 1.00)`
     - `Border = (0.28, 0.31, 0.35, 0.75)`
     - `FrameBg = (0.16, 0.17, 0.19, 1.00)`
     - `FrameBgHovered = (0.21, 0.24, 0.29, 1.00)`
     - `FrameBgActive = (0.24, 0.29, 0.35, 1.00)`
     - `Button = (0.18, 0.20, 0.23, 1.00)`
     - `ButtonHovered = (0.28, 0.47, 0.74, 0.85)`
     - `ButtonActive = (0.22, 0.43, 0.68, 1.00)`
     - `Header = (0.18, 0.22, 0.27, 0.88)`
     - `HeaderHovered = (0.28, 0.47, 0.74, 0.85)`
     - `HeaderActive = (0.22, 0.43, 0.68, 1.00)`
     - `Tab = (0.16, 0.18, 0.21, 1.00)`
     - `TabHovered = (0.28, 0.47, 0.74, 0.85)`
     - `TabSelected = (0.20, 0.26, 0.33, 1.00)`
   - `SoftLightTooling`
     - `Text = (0.18, 0.16, 0.14, 1.00)`
     - `WindowBg = (0.92, 0.90, 0.86, 1.00)`
     - `ChildBg = (0.95, 0.93, 0.89, 1.00)`
     - `PopupBg = (0.98, 0.96, 0.93, 0.99)`
     - `MenuBarBg = (0.88, 0.85, 0.80, 1.00)`
     - `Border = (0.50, 0.46, 0.40, 0.70)`
     - `FrameBg = (0.88, 0.85, 0.80, 1.00)`
     - `FrameBgHovered = (0.82, 0.78, 0.72, 1.00)`
     - `FrameBgActive = (0.77, 0.72, 0.66, 1.00)`
     - `Button = (0.84, 0.80, 0.74, 1.00)`
     - `ButtonHovered = (0.69, 0.60, 0.44, 1.00)`
     - `ButtonActive = (0.56, 0.48, 0.34, 1.00)`
     - `Header = (0.80, 0.75, 0.69, 0.92)`
     - `HeaderHovered = (0.69, 0.60, 0.44, 1.00)`
     - `HeaderActive = (0.56, 0.48, 0.34, 1.00)`
     - `Tab = (0.86, 0.82, 0.76, 1.00)`
     - `TabHovered = (0.69, 0.60, 0.44, 1.00)`
     - `TabSelected = (0.78, 0.73, 0.67, 1.00)`

5. While applying those values, keep the decision intent explicit in the resulting palette and spacing:
   - `Warm Studio Dark` should read as the D-05 likely final direction: warm charcoal and muted amber/olive emphasis, not blue-gray stock ImGui.
   - `Spectrum-Inspired Dark` should be slightly cooler and more neutral than `Warm Studio Dark`, but still restrained enough to satisfy D-14 and D-15 instead of becoming flashy.
   - `Soft Light Tooling` should preserve clear panel/input separation and readable borders so the light preset still behaves like tooling, not like a blank white document.

6. Implement `imguiThemePresetLabel()`, `requestThemePreset()`, and `applyPendingThemePreset()` in the same style as the existing font preset flow. Apply the theme in `beginFrame()` before `ImGui::NewFrame()` and before any editor windows are created in that frame.

7. Keep local semantic overrides intact per D-13: this task sets the global base `ImGuiStyle` only. Do not add `PushStyleColor` or `PushStyleVar` calls to `ImGuiLayer`.
  </action>
  <verify>
    <automated>cd /Users/ozan/Projects/gsd-3d-roguelike && rg -n "enum class ImGuiThemePreset|requestThemePreset|themePreset\\(\\) const|applyPendingThemePreset|WarmStudioDark|SpectrumInspiredDark|SoftLightTooling|StyleColorsLight\\(&style\\)" src/engine/ui/ImGuiLayer.h src/engine/ui/ImGuiLayer.cpp && cmake --build build --target level-editor</automated>
  </verify>
  <acceptance_criteria>
    - `src/engine/ui/ImGuiLayer.h` contains `enum class ImGuiThemePreset`
    - `src/engine/ui/ImGuiLayer.h` contains `WarmStudioDark`
    - `src/engine/ui/ImGuiLayer.h` contains `SpectrumInspiredDark`
    - `src/engine/ui/ImGuiLayer.h` contains `SoftLightTooling`
    - `src/engine/ui/ImGuiLayer.h` contains `void requestThemePreset(ImGuiThemePreset preset);`
    - `src/engine/ui/ImGuiLayer.h` contains `ImGuiThemePreset themePreset() const`
    - `src/engine/ui/ImGuiLayer.cpp` contains `applyPendingThemePreset();`
    - `src/engine/ui/ImGuiLayer.cpp` contains `ImGui::StyleColorsDark(&style);`
    - `src/engine/ui/ImGuiLayer.cpp` contains `ImGui::StyleColorsLight(&style);`
    - `src/engine/ui/ImGuiLayer.cpp` contains `0.12f, 0.11f, 0.10f`
    - `src/engine/ui/ImGuiLayer.cpp` contains `0.10f, 0.11f, 0.12f`
    - `src/engine/ui/ImGuiLayer.cpp` contains `0.92f, 0.90f, 0.86f`
    - `cmake --build build --target level-editor` exits 0
  </acceptance_criteria>
  <done>`ImGuiLayer` owns three concrete theme presets and can swap them globally before a new ImGui frame begins.</done>
</task>

<task type="auto">
  <name>Task 2: Add a live View -> Interface Theme menu that switches between the three approved presets</name>
  <files>
    apps/level_editor/main.cpp
  </files>
  <read_first>
    .planning/PROJECT.md
    .planning/phases/15-prototype-editor-imgui-themes-and-choose-a-final-skin-direction/15-CONTEXT.md
    apps/level_editor/main.cpp
    src/engine/ui/ImGuiLayer.h
  </read_first>
  <action>
Wire live theme switching into the existing editor menu structure per D-11 and D-04. Keep the UX parallel to the existing `Interface Font` menu and do not add a new settings window.

1. In `apps/level_editor/main.cpp`, add:
   ```cpp
   ImGuiThemePreset editorThemePreset = imgui.themePreset();
   ```
   immediately next to the existing `ImGuiFontPreset editorFontPreset = imgui.fontPreset();` state declaration.

2. In the `View` menu, add a new `Interface Theme` submenu immediately before the existing `Interface Font` submenu. Use this exact preset order:
   ```cpp
   static constexpr std::array<ImGuiThemePreset, 3> kThemePresets{
       ImGuiThemePreset::WarmStudioDark,
       ImGuiThemePreset::SpectrumInspiredDark,
       ImGuiThemePreset::SoftLightTooling,
   };
   ```

3. Each menu item must:
   - render its label with `imguiThemePresetLabel(preset)`
   - mark the currently selected preset with the existing `selected` bool pattern used by `Interface Font`
   - update both `editorThemePreset` and `imgui.requestThemePreset(preset)` when clicked

4. Keep the scope narrow per D-02 and D-09:
   - do not add a freeform color editor
   - do not add theme import/export
   - do not add custom widget rendering
   - do not move the selector out of the existing `View` menu

5. Do not add a separate help/debug/theme-lab window in the shipped UI for this task. The comparison surface is the live editor itself.
  </action>
  <verify>
    <automated>cd /Users/ozan/Projects/gsd-3d-roguelike && rg -n "Interface Theme|kThemePresets|WarmStudioDark|SpectrumInspiredDark|SoftLightTooling|requestThemePreset\\(preset\\)" apps/level_editor/main.cpp && cmake --build build --target level-editor</automated>
  </verify>
  <acceptance_criteria>
    - `apps/level_editor/main.cpp` contains `ImGuiThemePreset editorThemePreset = imgui.themePreset();`
    - `apps/level_editor/main.cpp` contains `BeginMenu("Interface Theme")`
    - `apps/level_editor/main.cpp` contains `static constexpr std::array<ImGuiThemePreset, 3> kThemePresets`
    - `apps/level_editor/main.cpp` contains `ImGuiThemePreset::WarmStudioDark`
    - `apps/level_editor/main.cpp` contains `ImGuiThemePreset::SpectrumInspiredDark`
    - `apps/level_editor/main.cpp` contains `ImGuiThemePreset::SoftLightTooling`
    - `apps/level_editor/main.cpp` contains `imguiThemePresetLabel(preset)`
    - `apps/level_editor/main.cpp` contains `imgui.requestThemePreset(preset);`
    - `cmake --build build --target level-editor` exits 0
  </acceptance_criteria>
  <done>The level editor can switch among the three approved theme presets live from the View menu without a restart.</done>
</task>

</tasks>

<verification>
1. `ImGuiLayer` owns a real `ImGuiThemePreset` API and applies theme changes before `ImGui::NewFrame()`
2. The level editor exposes `View -> Interface Theme` with exactly three items in the user-approved order
3. `cmake --build build --target level-editor` succeeds after the theme infrastructure and menu wiring land
</verification>

<success_criteria>
- The editor has three live-switchable ImGui themes implemented as repo-local preset code on top of upstream Dear ImGui
- Theme switching is global, immediate, and compatible with the editor's existing local style overrides
- The menu surface is minimal and reversible: one View submenu, no new framework, no extra theming UI
</success_criteria>

<output>
After completion, create `.planning/phases/15-prototype-editor-imgui-themes-and-choose-a-final-skin-direction/15-01-SUMMARY.md`
</output>
