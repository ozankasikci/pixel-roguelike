---
phase: 01.1-project-restructure-ecs-application-class-modular-engine-game-split
plan: 01
subsystem: infra
tags: [cmake, entt, opengl, c++20, modular-build]

# Dependency graph
requires:
  - phase: 01-engine-and-dithering-pipeline
    provides: All source files (Window, ImGuiLayer, Screenshot, Shader, Framebuffer, DitherPass, Mesh, Renderer, CathedralScene)
provides:
  - Modular directory structure (engine/core, engine/rendering, engine/ui, game/scenes)
  - Per-module CMake static libraries (engine_core, engine_rendering, engine_ui, game)
  - EnTT v3.16.0 available via FetchContent as EnTT::EnTT
  - Empty placeholder dirs for future plans (engine/{input,ecs,scene}, game/{components,systems})
affects: [01.1-02, 01.1-03, 01.1-04, all subsequent plans in phase 01.1]

# Tech tracking
tech-stack:
  added: [EnTT v3.16.0 (FetchContent)]
  patterns: [Per-module static library CMake pattern with PUBLIC include_dirs for transitive consumption, modular src/ directory hierarchy]

key-files:
  created:
    - src/engine/core/Window.h
    - src/engine/core/Window.cpp
    - src/engine/rendering/Shader.h
    - src/engine/rendering/Shader.cpp
    - src/engine/rendering/Framebuffer.h
    - src/engine/rendering/Framebuffer.cpp
    - src/engine/rendering/DitherPass.h
    - src/engine/rendering/DitherPass.cpp
    - src/engine/rendering/Mesh.h
    - src/engine/rendering/Mesh.cpp
    - src/engine/rendering/Renderer.h
    - src/engine/rendering/Renderer.cpp
    - src/engine/ui/ImGuiLayer.h
    - src/engine/ui/ImGuiLayer.cpp
    - src/engine/ui/Screenshot.h
    - src/engine/ui/Screenshot.cpp
    - src/game/scenes/CathedralScene.h
    - src/game/scenes/CathedralScene.cpp
  modified:
    - CMakeLists.txt
    - src/main.cpp

key-decisions:
  - "Per-module static library CMake pattern: each lib uses PUBLIC target_include_directories(${CMAKE_SOURCE_DIR}/src) so all downstream consumers automatically get the include path"
  - "EnTT v3.16.0 added via FetchContent; not used yet but available as EnTT::EnTT for plan 02"
  - "Duplicate library linker warning on macOS (ld: ignoring duplicate libraries) is expected and harmless — caused by transitive deps appearing multiple times, not a build error"

patterns-established:
  - "Modular source layout: src/engine/{core,rendering,ui,input,ecs,scene}/, src/game/{scenes,components,systems}/"
  - "CMake pattern: add_library(<module> STATIC ...) with target_include_directories(PUBLIC ${CMAKE_SOURCE_DIR}/src) and explicit dependency chain"

requirements-completed: []

# Metrics
duration: 7min
completed: 2026-03-23
---

# Phase 01.1 Plan 01: Project Restructure - Modular Directory and CMake Summary

**Reorganized source tree into engine/game modules, restructured CMake into per-module static libraries, and added EnTT v3.16.0 via FetchContent — binary compiles and runs identically**

## Performance

- **Duration:** 7 min
- **Started:** 2026-03-23T18:26:27Z
- **Completed:** 2026-03-23T18:33:00Z
- **Tasks:** 2
- **Files modified:** 20 (19 moved/renamed + CMakeLists.txt)

## Accomplishments

- All source files moved to new modular directory structure using git mv (history preserved)
- CMake restructured from monolithic executable to 4 per-module static libraries with explicit dependency chains
- EnTT v3.16.0 integrated via FetchContent and available for plan 02
- Binary compiles with zero errors and produces identical screenshot output

## Task Commits

Each task was committed atomically:

1. **Task 1: Move source files to new directory structure and update all includes** - `d3844d8` (feat)
2. **Task 2: Restructure CMake into per-module static libraries and add EnTT FetchContent** - `7fa8c82` (feat)

**Plan metadata:** `84a4e74` (docs: complete plan)

## Files Created/Modified

- `src/engine/core/Window.h/.cpp` - Window class relocated from engine/ to engine/core/
- `src/engine/rendering/` - Shader, Framebuffer, DitherPass, Mesh, Renderer relocated from renderer/
- `src/engine/ui/ImGuiLayer.h/.cpp` - ImGuiLayer relocated from engine/ to engine/ui/
- `src/engine/ui/Screenshot.h/.cpp` - Screenshot relocated from engine/ to engine/ui/
- `src/game/scenes/CathedralScene.h/.cpp` - CathedralScene relocated from game/ to game/scenes/
- `src/main.cpp` - All includes updated to new paths
- `CMakeLists.txt` - Restructured into per-module static libs with EnTT FetchContent

## Decisions Made

- Used `PUBLIC target_include_directories(${CMAKE_SOURCE_DIR}/src)` on each library so downstream targets automatically inherit the include path without needing extra PRIVATE declarations
- EnTT::EnTT linked to `game` lib and executable now so it's ready for plan 02 ECS work
- macOS linker duplicate library warning is expected and harmless — no fix needed

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- macOS linker emits `ld: warning: ignoring duplicate libraries: 'libengine_core.a', 'libengine_rendering.a'` — this is a harmless warning caused by transitive static library dependencies appearing in the link line more than once. Not an error, binary is produced correctly.

## Known Stubs

None - all functionality from phase 01 is preserved. No new stubs introduced.

## Next Phase Readiness

- Directory structure matches D-01/D-02/D-03 exactly as required by plans 02-04
- EnTT available for plan 02 (ECS introduction)
- Empty directories created for future plans: engine/{input,ecs,scene}, game/{components,systems}
- No blockers for plan 02

## Self-Check: PASSED

- FOUND: src/engine/core/Window.h
- FOUND: src/engine/rendering/Renderer.h
- FOUND: src/engine/ui/ImGuiLayer.h
- FOUND: src/game/scenes/CathedralScene.h
- FOUND: CMakeLists.txt
- FOUND: 01.1-01-SUMMARY.md
- FOUND commit d3844d8: Move source files to modular directory structure
- FOUND commit 7fa8c82: Restructure CMake into per-module static libs, add EnTT FetchContent

---
*Phase: 01.1-project-restructure-ecs-application-class-modular-engine-game-split*
*Completed: 2026-03-23*
