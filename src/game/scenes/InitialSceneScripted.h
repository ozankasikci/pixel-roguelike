#pragma once

/// Register scripted geometry for initial_scene (interactive doors).
/// Must be called at startup before any GenericFileScene loads initial_scene.
void registerInitialSceneScripted();
